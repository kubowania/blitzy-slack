/**
 * PostCSS configuration for the `@app/web` package.
 *
 * Tailwind CSS v4 is processed by the `@tailwindcss/vite` plugin (see
 * vite.config.ts), so no Tailwind PostCSS plugin is required here. This config
 * is an intentional no-op.
 */
/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {},
};
