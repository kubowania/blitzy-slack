# Decision Log

This document is the single source of truth for **why** non-trivial implementation
decisions were made in the Blitzy Slack proof-of-concept. Per the project's
Explainability rule, rationale MUST live here — **not** in code comments. A decision
is "non-trivial" if a competent engineer could reasonably have chosen differently.
Any deviation from a literal or obvious interpretation of the requirements MUST have
an explicit entry here; unexplained deviations are treated as defects.

Downstream code-generation agents MUST append a row to the relevant table below for
every additional non-trivial decision they make.

## How to use this log

- **Scope — every non-trivial decision.** A decision is "non-trivial" if a competent engineer could reasonably have chosen differently. Each one gets a row in the relevant section below.
- **Append-only.** Rows are never edited or removed. If a decision is later reversed or refined, add a NEW row in the same section that names the decision it supersedes, rather than rewriting history.
- **Code comments carry HOW, this log carries WHY.** Comments may describe mechanics, edge cases, and non-obvious control flow; rationale and trade-offs belong here. This keeps the "why" out of line-level `git blame` churn and in one auditable place (AAP §0.8.3).
- **Deviations are defects unless logged.** Any departure from a literal or obvious reading of the Agent Action Plan (AAP) MUST have an explicit row explaining it.
- **Organisation.** Decisions are grouped by the package or concern they belong to — one `##` section per area — and each section holds a four-column table: _What was decided_ · _What alternatives existed_ · _Why this choice was made_ · _What risks it carries_. Add a new `##` section for a new area, or append to an existing section when one already fits.
- **Adding a row.** Append to the bottom of the matching section's table, fill all four columns, wrap code identifiers in backticks, and escape any literal pipe inside a cell as `\|`.

## Decision-log format

| What was decided                                                                                                                                                                                                    | What alternatives existed                                                                                                    | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                | What risks it carries                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| This log is organised as one four-column table per package/concern — the format already established by every section below — rather than a single flat table keyed by a sequential `#` with `Date`/`Author` columns | A single global table with `#`/`Date`/`Author` columns (the shape sketched in this file's planning seed)                     | The per-section format was the convention already in use by every committed entry when this section was authored. Preserving it keeps the existing decisions intact (append-only, AAP §0.8.3) and co-locates related decisions by package as the codebase grows. Rewriting into a flat numbered table would have deleted committed rationale and reformatted history, which the Explainability rule forbids             | Rows cannot be referenced by a global index ("decision N"); they are instead referenced by their section heading plus _What was decided_ text. Acceptable for an append-only, package-grouped log |
| Three planning-seed decisions were deliberately NOT adopted because the implemented code chose differently; the authoritative rows are the ones already in this log                                                 | Record the seed values verbatim: `engines.node >=22.11.0`, explicit package enumeration, and `@blitzy-slack/*` package scope | A decision log must match shipped code. (1) The workspace uses the `packages/*` wildcard — see the _Root workspace & tooling_ wildcard row — not explicit enumeration. (2) Packages are scoped `@app/*` — see the `@app/db` and `@app/web` rows — not `@blitzy-slack/*`. (3) `engines.node` is `>=22.12.0` — see the row below — not `>=22.11.0`. Recording the seed values would have made the log contradict the code | A reader expecting the planning-seed values will not find them as adopted decisions; this row is the explicit pointer that reconciles seed intent with the implemented choices                    |

## Root workspace & tooling

| What was decided                                                                                                                                                                                                                  | What alternatives existed                                                                                                                                                                                                                                             | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | What risks it carries                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| pnpm workspace declared with the `packages/*` wildcard in `pnpm-workspace.yaml`                                                                                                                                                   | Enumerate each package path explicitly (`packages/db`, `packages/shared`, `packages/api`, `packages/web`)                                                                                                                                                             | The wildcard is the AAP/checkpoint-specified workspace contract and automatically includes any future package added under `packages/` without editing the manifest. Explicit enumeration silently omits new packages and drifts from the spec.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A stray non-package directory under `packages/` would be matched by the glob; acceptable because that tree holds only the four intended workspaces.                                                                                                                                                                                                               |
| `onlyBuiltDependencies` allow-list declared in `pnpm-workspace.yaml` (`@prisma/client`, `prisma`, `bcrypt`, `esbuild`, `@playwright/test`)                                                                                        | Rely on interactive `pnpm approve-builds`; disable build-script gating entirely                                                                                                                                                                                       | pnpm 10+ refuses to run a dependency's install/build scripts unless it is allow-listed (pnpm 9 accepts the field without complaint), so without the list `prisma generate` and `bcrypt`'s native binding never build and `make local` (Gate 1) fails on a clean checkout                                                                                                                                                                                                                                                                                                                                                                                                                                                                | A new native or install-script dependency that is omitted from the list fails silently — its binding is simply not built; the list must be updated whenever such a dependency is introduced                                                                                                                                                                       |
| Root `package.json` sets `"type": "module"` so the workspace root is ESM by default                                                                                                                                               | CommonJS by default (omit the field) and use `.cjs` for tooling configs                                                                                                                                                                                               | `eslint.config.js`, `vite.config.ts`, and `playwright.config.ts` use `import`/`export`, and `tsconfig.base.json` sets `module: NodeNext`; ESM at the root keeps the toolchain consistent and avoids per-file `.mjs`/`.cjs` juggling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | A tool that assumes a bare `.js` config is CommonJS could break; mitigated by renaming any such file to `.cjs`                                                                                                                                                                                                                                                    |
| Root `engines.node` pinned to `>=22.12.0`, with `.nvmrc` selecting the same line                                                                                                                                                  | The AAP's stated floor `>=22.0.0`; Prisma's hard floor `>=22.11.0`                                                                                                                                                                                                    | Prisma 6 requires Node `>=22.11.0`, so the AAP's `>=22.0.0` would let an unsupported Node install and then fail at `prisma generate`/runtime; `22.12.0` is the current Node 22 LTS patch and the value `.nvmrc` selects, so `engines` and `.nvmrc` stay in agreement                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Developers on Node 22.0–22.11 cannot `pnpm install`; mitigated by `.nvmrc` driving `nvm use` / `fnm use` to the pinned version                                                                                                                                                                                                                                    |
| ESLint 9 **flat config** authored in `eslint.config.js`, with `packages/web/src/components/ui/**` placed in `ignores`                                                                                                             | Legacy `.eslintrc.cjs`; an `eslint.config.mjs` variant                                                                                                                                                                                                                | ESLint 9 and `typescript-eslint` v8 are flat-config-first, and the ESM root lets a `.js` flat config load natively (Rule 3 runs `eslint . --max-warnings 0`); the shadcn-generated `ui/**` primitives are copied verbatim and owned upstream, so they are ignored rather than linted against project rules                                                                                                                                                                                                                                                                                                                                                                                                                              | Editor/CI integrations that only understand `.eslintrc` will not read this config; mitigated by pinning ESLint 9 across environments. Ignoring `ui/**` means a defect inside a generated primitive is not caught by lint                                                                                                                                          |
| Prettier configured with `printWidth: 100` (plus `singleQuote: true`, `trailingComma: "all"`, `endOfLine: "lf"`)                                                                                                                  | Prettier's default `printWidth: 80`; a wider `120`                                                                                                                                                                                                                    | 100 columns suit modern wide displays while staying readable in side-by-side diffs on laptops, and the value is common across the TypeScript ecosystem; `make format` / `prettier --check` enforces it uniformly, including the table alignment in this Markdown file                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Contributors used to 80 columns may find some lines wide on small screens; immaterial because the formatter rewrites everything to the policy                                                                                                                                                                                                                     |
| `tsconfig.base.json` enables `noUncheckedIndexedAccess: true`, beyond the strict flags the AAP lists                                                                                                                              | Only the AAP-listed flags (`strict`, `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`, `forceConsistentCasingInFileNames`)                                                                                                                        | Typing `arr[i]` as `T \| undefined` catches a common class of runtime "undefined is not …" bugs at compile time, reinforcing Rule 3's strictness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Array/record access needs explicit guards or a `!` assertion (the latter justified in this log where used); accepted in exchange for the safety win                                                                                                                                                                                                               |
| `tsconfig.base.json` leaves `exactOptionalPropertyTypes: false` (the TypeScript default)                                                                                                                                          | Enable `exactOptionalPropertyTypes: true` for stricter optional-property semantics                                                                                                                                                                                    | Zod infers optional fields as `T \| undefined`, which clashes with the stricter `T?` discriminant `exactOptionalPropertyTypes: true` demands; enabling it would force hand-written type adapters across every Zod-inferred DTO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `{ foo: undefined }` and `{}` are treated alike by the checker; mitigated because Zod validates the same shapes at runtime                                                                                                                                                                                                                                        |
| The `make local` API-readiness gate is an **inline POSIX-shell loop** in the `Makefile` (`for i in $(seq 1 60); do curl -sf "$API_HEALTH_URL" …; sleep 2; done`) rather than a separate `scripts/wait-for-health.sh`              | A dedicated wait-for-health script file; a `tsx` Node waiter; relying on `docker compose --wait` (which covers only the DB/Redis containers, not the host API)                                                                                                        | Keeps the committed file inventory minimal and the polling portable across bash/dash/zsh; the loop blocks `make seed` and the web start-up until `GET /api/health` returns 200, satisfying Rule 4 ("seed only after the API is healthy") and Gate 1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Windows `make` users need WSL or Git-Bash with `curl` on `PATH`; documented as a prerequisite in the README                                                                                                                                                                                                                                                       |
| `eslint.config.js` block 9 (the `disableTypeChecked` group for files outside any package tsconfig) extended so its `files` glob also matches `**/prisma/seed.ts`, linting the Prisma `db seed` delegate with syntactic rules only | (a) Add `prisma/seed.ts` to `packages/db/tsconfig.json` `include` so the type-aware project service can resolve it; (b) leave the config untouched and accept that `eslint .` cannot lint the file; (c) add the file to a `projectService` `allowDefaultProject` list | `packages/db/prisma/seed.ts` is deliberately excluded from every tsconfig (it runs from source via `tsx`, never compiled), so `typescript-eslint`'s `projectService: true` rejects it at parse time ("not found by the project service") and `eslint . --max-warnings 0` (Rule 3) fails. (a) would force the file into `tsc`/`dist` output, which the design forbids; (b) leaves a file silently un-lintable, defeating Rule 3; (c) is functionally equivalent, but `disableTypeChecked` already covers exactly this category (root `*.config.ts`, `scripts/**`), so extending that one glob is the established minimal pattern. Verified: full-workspace `eslint .` stays green and the glob matches only `packages/db/prisma/seed.ts` | Type-aware rules (`no-floating-promises`, `consistent-type-imports`) do not run on the delegate; acceptable because it is a tiny dependency-free `child_process.spawn` shim whose correctness is covered by syntactic lint plus a runtime smoke test. Any future type-checked `*.ts` named `seed.ts` under a `prisma/` dir would likewise skip type-aware linting |

## Infrastructure (`docker-compose.yml`)

| What was decided                                                            | What alternatives existed                                      | Why this choice was made                                                                                                                                                                                  | What risks it carries                                                                                                                                                      |
| --------------------------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| No top-level `version:` field in `docker-compose.yml`                       | Declare `version: '3.x'` as in legacy Compose files            | The Compose Specification (Compose v2) ignores `version:` and emits an "obsolete" warning when it is present; omitting it is the current-format default.                                                  | Standalone `docker-compose` v1 binaries would not parse the file, but the project targets Compose v2 (`docker compose`).                                                   |
| `postgres:16-alpine` and `redis:7-alpine` images                            | `postgres:16` / `redis:7` (Debian-based), or pinned patch tags | Alpine variants are the smallest official images, speeding first-run pulls for the local PoC; the `16`/`7` majors match the AAP-required engine versions.                                                 | Floating on the minor tag means `docker compose pull` can advance the patch level. No native extensions compile against these images, so musl vs glibc is immaterial here. |
| PostgreSQL initialised as UTF-8 with `C` collation (`POSTGRES_INITDB_ARGS`) | Default locale collation (e.g. `en_US.UTF-8`)                  | `C` collation gives stable byte-level ordering identical across host locales, keeping cursor pagination deterministic between developer machines. Search ranking is handled by `tsvector`, not collation. | `C` collation sorts bytes, not linguistic order; acceptable because ordering keys are timestamps/ids. Applied only on first volume initialisation.                         |
| Redis started with AOF persistence (`--appendonly yes`)                     | RDB snapshotting (default) or no persistence                   | AOF persists presence keys and Socket.io adapter state across container restarts with a tighter durability window than periodic RDB snapshots.                                                            | AOF files grow larger than RDB and add minor write overhead; negligible at PoC scale.                                                                                      |
| API and web run on the host; only Postgres and Redis run in Compose         | Containerise the API and web servers too                       | Running the Node servers on the host keeps Vite/tsx hot-reload fast and avoids bind-mount/file-watch friction inside containers for the local PoC.                                                        | The host must have Node 22 + pnpm installed (documented in the README). Container/production parity is out of PoC scope.                                                   |
| Named volumes (`blitzy-slack-postgres-data`, `blitzy-slack-redis-data`)     | Anonymous volumes, or host bind mounts                         | Stable, predictable names survive `make down`/`make clean` (neither passes `-v`) and are independent of the Compose project directory.                                                                    | Data persists until explicitly removed with `docker compose down -v`; a developer expecting a clean slate must run that command.                                           |

## `packages/shared` — Shared contracts

| What was decided                                                                                                                                                                                                                                                                 | What alternatives existed                                                                                      | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                         | What risks it carries                                                                                                                                                           |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Socket.io event names exported as `as const` string constants (`events.ts`) rather than a TypeScript `enum`                                                                                                                                                                      | A TypeScript `enum`, or a plain (non-`as const`) object                                                        | `as const` literals erase at compile time (zero runtime cost, no emitted object) and give exact literal types for the typed `ClientToServerEvents`/`ServerToClientEvents` contract; an `enum` emits a runtime object and is awkward to use as a union of literals.                                                                                                                                                                                                               | Consumers import the constants rather than a runtime enum namespace; this matches how both client and server reference the shared contract.                                     |
| The error event constant is exported as `ERROR = 'error'` (no `SOCKET_ERROR` alias)                                                                                                                                                                                              | Keep the prior `SOCKET_ERROR` name, or export both                                                             | The checkpoint/shared contract specifies `ERROR`; socket typings and imports are generated against that name. A second alias would create two ways to reference one wire event and risk drift.                                                                                                                                                                                                                                                                                   | Any code referencing `SOCKET_ERROR` must switch to `ERROR`; no such consumers exist at this checkpoint.                                                                         |
| `PresenceState` modelled as a string-literal union (`'online' \| 'away' \| 'offline'`)                                                                                                                                                                                           | A TypeScript `enum`                                                                                            | Same reasoning as the event constants: a union of string literals is zero-runtime, serialises directly over the wire/JSON, and narrows cleanly.                                                                                                                                                                                                                                                                                                                                  | Values are bare strings, so a typo is caught only where the `PresenceState` type is applied; usage sites are typed, so this is contained.                                       |
| User DTOs modelled as four shapes — `User` (internal, includes `passwordHash`), `UserResponse = Omit<User,'passwordHash'>` (authenticated self), `PublicUser` (peer view, omits `email`/`passwordHash`), `AuthenticatedUser extends PublicUser` (adds `email`)                   | A single `User` type without `passwordHash`, or overloading `User` for both DB row and wire response           | The four-shape contract is the checkpoint/AAP §0.4.4 specification: `passwordHash` stays on the server-only `User` shape, the self-view exposes `email` but no hash, and the peer-view omits `email`. Overloading one type for DB and wire blurs the privacy boundary and risks leaking `passwordHash`.                                                                                                                                                                          | More named types to keep aligned with the Prisma model; mitigated because all derive from `User` via `Omit`/`extends`, so one source change propagates.                         |
| Zod request schemas use `.strict()` to reject unknown keys (e.g. `startDmSchema` in `schemas/dm.ts`)                                                                                                                                                                             | Default Zod object parsing (unknown keys silently stripped); `.passthrough()` (unknown keys kept)              | `.strict()` rejects over-posting / prototype-pollution attempts at the validation boundary instead of silently dropping unexpected fields, so a malformed or hostile body fails fast with a clear 400 (Gate 12)                                                                                                                                                                                                                                                                  | A client that sends extra cosmetic fields gets a 400; acceptable because every request shape is defined by the shared schema and the web forms submit exactly those fields      |
| Numeric limits centralised as `as const` constants in `constants/limits.ts` (`PAGE_SIZE = 50`, `MAX_PAGE_SIZE = 100`, `MAX_FILE_SIZE_MB = 10`, `HEARTBEAT_INTERVAL_MS = 30_000`, `ONLINE_THRESHOLD_MS = 60_000`, `AWAY_THRESHOLD_MS = 300_000`, plus message/channel/emoji caps) | Hardcode each value at its use site across `api` and `web`; read every limit from the environment              | One shared module is the single source of truth so the API validator, the DB `VARCHAR(n)` caps, and the web's fail-fast UI all agree on the same numbers (AAP §0.8.4); the env loader layers a runtime override only for `MAX_FILE_SIZE_MB`, where tuning is actually needed                                                                                                                                                                                                     | If a limit ever needs to differ between client and server a single constant cannot express that; none currently do, and the env loader covers the one runtime-tunable value     |
| `sendMessageSchema` enforces the channel/DM XOR with `.superRefine`, not `.refine`                                                                                                                                                                                               | A single `.refine` returning one boolean with a generic message                                                | `.superRefine` attaches a field-specific issue to BOTH `channelId` and `dmId` (distinct messages for the "both set" vs "neither set" cases), so the API and the web form surface the error on the correct field; a single `.refine` can only emit one form-level message (CP2 review remediation, M26).                                                                                                                                                                          | Two issues are emitted when the XOR fails, so a consumer counting issues must expect both `channelId` and `dmId` paths; intended for clearer field-level feedback.              |
| `reactionSchema.emoji` validated by a Unicode-emoji regex (`isStandardEmoji`) in addition to the `.min(1).max(MAX_EMOJI_LENGTH)` length bound                                                                                                                                    | Length-only validation; a hardcoded allow-list of specific emoji; `Intl.Segmenter` single-grapheme enforcement | A length-only check accepted arbitrary text as a "reaction"; the regex requires every code point to be emoji-related (`\p{Emoji}`, VS16, ZWJ, keycap, tag chars) AND at least one pictograph / regional-indicator / keycap, so plain text, digits, and punctuation are rejected while skin-tone, ZWJ sequences, flags, and keycaps pass (CP2 review remediation, M27). `Intl.Segmenter` was rejected to avoid a `lib.esnext.intl` type dependency and Firefox <125 runtime risk. | Allows multiple emoji in one value, bounded only by `MAX_EMOJI_LENGTH` (64); a single-grapheme cap can be layered later if the product requires exactly one emoji per reaction. |
| `FileAttachment.uploadedById` typed `string \| null` (not `string`)                                                                                                                                                                                                              | Keep it non-nullable `string` and assume an uploader always exists                                             | The `File.uploadedById` relation is `ON DELETE SET NULL` (see the migrations section), so an API response can legitimately carry `null` after the uploader is deleted; the DTO must mirror the nullable column (CP2 review remediation, M28).                                                                                                                                                                                                                                    | UI and API consumers must handle a missing uploader (`null`) — e.g. render "Unknown" — rather than assuming a user id is always present.                                        |

## `packages/db` — Database package

| What was decided                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | What alternatives existed                                                                                                                                                                                                                     | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | What risks it carries                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Singleton `PrismaClient` via a `globalThis` guard in `packages/db/src/client.ts`                                                                                                                                                                                                                                                                                                                                                                                                                              | (a) A factory function returning fresh instances; (b) A module-level constant only, with no `globalThis` cache                                                                                                                                | Option (a) fragments the connection pool across test files and breaks Prisma's intended single-instance model; option (b) leaves a window where `tsx watch` and test-runner hot-reload can double-construct the client. The `globalThis` guard handles both production correctness and dev-mode hot-reload safety.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Couples the module to Node-specific `globalThis` semantics. Acceptable because the package is Node-only (server-side).                                                                                                                                                                                                                                |
| Prisma logging fixed to `log: ['warn', 'error']`                                                                                                                                                                                                                                                                                                                                                                                                                                                              | (a) `['query', 'info', 'warn', 'error']` for verbose dev; (b) `['error']` only                                                                                                                                                                | AAP §0.6.1 Group 2 mandates `['warn', 'error']` verbatim. Verbose `query` logging would flood the Pino output and obscure real signals (Gate 10 — Observability). Suppressing warnings would hide deprecations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | If a hot debugging scenario needs query-level logs, a developer must temporarily edit `client.ts`. Documented here as the trade-off.                                                                                                                                                                                                                  |
| Graceful shutdown via an exported `async disconnectPrisma()` plus `process.once('SIGTERM'\|'SIGINT')` handlers that `await` the disconnect (inside a re-entrancy guard) and then re-raise the original signal with `process.kill(process.pid, signal)`                                                                                                                                                                                                                                                        | (a) `process.on` with fire-and-forget `void prisma.$disconnect()`; (b) `beforeExit` only; (c) no shutdown hook; (d) export `disconnectPrisma()` with no in-package signal handlers, letting the API layer own all signal handling             | `process.on` + `void` (a) swallows disconnect failures and, by registering a listener, overrides Node's default signal termination so the process can hang or surface unhandled rejections; `beforeExit` (b) never fires on signal kill (`docker compose down`, `make down`); (c) orphans PostgreSQL connections, eventually causing "too many connections". `process.once` + `await` guarantees the disconnect runs exactly once and completes, and re-raising the now-removed signal restores default termination. The exported `disconnectPrisma()` additionally lets the API server's top-level shutdown path coordinate HTTP/socket closure with the Prisma disconnect (subsuming option (d)).                                                                                                                                                                                                                          | A failed disconnect is swallowed best-effort (the logger may already be torn down). Re-raising depends on `once` having removed the handler, so a repeated signal terminates immediately — intended.                                                                                                                                                  |
| JSDoc references in `client.ts` use the package name `@app/db` rather than the AAP's illustrative `@blitzy-slack/db`                                                                                                                                                                                                                                                                                                                                                                                          | Keep the literal `@blitzy-slack/db` string from the agent prompt                                                                                                                                                                              | The actual workspace package name (set during setup in `packages/db/package.json`) is `@app/db`. Documentation must match the real import specifier so consumers are not misled. Functional code is unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | None functional; this is a documentation-accuracy alignment. If the package is ever renamed, the JSDoc must be updated alongside `package.json`.                                                                                                                                                                                                      |
| `packages/db/src/index.ts` barrel re-exports the 8 Prisma-generated model TYPES (`User`, `Channel`, `ChannelMember`, `DirectMessage`, `DMParticipant`, `Message`, `MessageReaction`, `File`) directly from `@prisma/client` via `export type { … }`                                                                                                                                                                                                                                                           | (a) Redefine the entity DTOs in `@app/shared`; (b) force every consumer to import them from `@prisma/client` directly                                                                                                                         | (a) duplicates the model surface and must be hand-synced with every schema migration; (b) leaks `@prisma/client` into every API service file. Re-exporting from the db barrel keeps `schema.prisma` the single source of truth and centralises the import surface in one module. `export type` (not `export`) is required because `isolatedModules` (TS1205) and the `consistent-type-imports` ESLint rule both demand the `type` keyword for type-only re-exports                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Tight coupling to Prisma's generated type shape — a Prisma 6→7 type-shape change would need fixes in consumer code. Acceptable given Prisma's stable type contract                                                                                                                                                                                    |
| The `@app/db` barrel re-exports the `PrismaClient` class as a runtime VALUE (plain `export`), not merely as a type                                                                                                                                                                                                                                                                                                                                                                                            | Re-export it type-only via `export type`                                                                                                                                                                                                      | Tests construct isolated client instances with custom datasource URLs (`new PrismaClient({ datasources: { db: { url } } })`) for parallel test isolation; a value re-export enables this through `@app/db` without leaking `@prisma/client` into test files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Negligibly larger compiled-output footprint                                                                                                                                                                                                                                                                                                           |
| The `@app/db` barrel re-exports the `Prisma` namespace alongside the `PrismaClient` class                                                                                                                                                                                                                                                                                                                                                                                                                     | Force consumers to `import type { Prisma } from '@prisma/client'` themselves                                                                                                                                                                  | The `Prisma` namespace supplies generated input/where types (`Prisma.UserCreateInput`, `Prisma.MessageWhereInput`) and the `PrismaClientKnownRequestError` error family used heavily by `@app/api` services; centralising the re-export prevents `@prisma/client` from leaking into service code                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Marginally larger barrel surface                                                                                                                                                                                                                                                                                                                      |
| `packages/db/src/index.ts` re-exports BOTH `prisma` AND `disconnectPrisma` from `./client.js`, although the `index.ts` file schema/agent-prompt enumerate only `prisma`                                                                                                                                                                                                                                                                                                                                       | Re-export only `prisma`, leaving `disconnectPrisma` reachable solely through the `@app/db/client` subpath export                                                                                                                              | `client.ts` documents its public contract as "Consumers import `prisma` (and, for shutdown, `disconnectPrisma`) from `@app/db`", and the graceful-shutdown row above notes `disconnectPrisma()` lets the API server's shutdown path coordinate HTTP/socket closure with the Prisma disconnect. Since this barrel IS the `@app/db` entry point, re-exporting `disconnectPrisma` honours that documented surface and prevents `import { disconnectPrisma } from '@app/db'` from breaking once the API is authored. Re-exporting an extra value from a whitelisted dependency violates no rule, and the schema export list is a required minimum, not an exhaustive cap                                                                                                                                                                                                                                                         | One symbol beyond the schema's literal enumeration; immaterial since it is a documented part of the package's intended public API                                                                                                                                                                                                                     |
| `packages/db/prisma/seed.ts` (the `prisma db seed` entry resolved from the `prisma.seed` field) delegates to the single-source-of-truth `scripts/seed-via-api.ts` via `child_process.spawn('tsx', [scriptPath])` with `stdio: 'inherit'`, `env: process.env`, `shell: false`, propagating the child exit code through `process.exit`; `scriptPath` is computed cwd-independently from `import.meta.url` as `resolve(dirname(fileURLToPath(import.meta.url)), '..', '..', '..', 'scripts', 'seed-via-api.ts')` | (a) `await import(scriptPath)` to run the root script in-process; (b) set `prisma.seed` directly to `tsx ../../scripts/seed-via-api.ts` with no delegate file; (c) `exec`/`execSync`/`fork`; (d) a hardcoded relative path or `process.cwd()` | Rule 4 (AAP §0.8.1) forbids a direct DB insert, so the delegate must defer to the registration-flow script; `spawn` reproduces the separate-process semantics of `make seed` (same env, exit codes, streamed stdio) and is robust to whether the root script is top-level-executable or function-exporting. (a) shares the process, so the root script's top-level `process.exit` would pre-empt the delegate and its CLI detection (`argv[1] === import.meta.url`) would misfire; (b) is fragile because Prisma changes cwd before seeding and `..` traversal in `prisma.seed` has historically broken; (c) `exec`/`execSync` buffer rather than stream and `fork` needs a Node IPC entry point `tsx` is not; (d) `process.cwd()` is unreliable under Prisma, so `import.meta.url` is the cwd-independent ESM-native form. `__filename`/`__dirname` are inlined, not declared, to avoid `no-redeclare` against Node globals | A child `tsx` process adds ~200 ms startup over an in-process import (negligible for a one-shot seed); bare `tsx` must be on `PATH` (guaranteed in the pnpm/Prisma contexts that run the seed, and forwarded via `env: process.env`); the three `..` levels assume the fixed `packages/db/prisma/` layout, so a directory move requires updating them |

## `packages/db` — Prisma migrations

| What was decided                                                                                                                                                                                                                                                                      | What alternatives existed                                                                                                       | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | What risks it carries                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Full-text search column named `contentTsv` (index `Message_contentTsv_idx`)                                                                                                                                                                                                           | The earlier `contentSearch` / `Message_contentSearch_idx` names                                                                 | `contentTsv` is the checkpoint/AAP-specified name that later search queries (`prisma.$queryRaw` in `search.service.ts`) are written against. A mismatched name fails at runtime with a missing-column error.                                                                                                                                                                                                                                                                                                                                             | The generated column must stay in sync with the Prisma schema authored in CP2; the migration is the source of truth until then.                                                                                             |
| 1:1 DM uniqueness enforced via sorted `participantOneId`/`participantTwoId` columns on `DirectMessage` plus a unique pair index, retaining `DMParticipant` for membership                                                                                                             | (a) Rely only on the `DMParticipant(dmId,userId)` PK; (b) a single canonical `participantKey` string column with a unique index | The `DMParticipant` PK only prevents duplicate participants inside one DM — it does not stop two `DirectMessage` rows holding the same pair, violating the AAP "unique composite on the participant pair". Sorted columns + a unique index give deterministic DB-level enforcement and stay queryable; a concatenated key (b) is opaque and harder to join on.                                                                                                                                                                                           | The service layer MUST canonicalise the pair (sort the two user ids) before insert so the unique index is effective — an application invariant the CP2 service code must honour.                                            |
| Message cursor indexes created `createdAt DESC, id DESC` (the `channelId`/`dmId` composites and the standalone `createdAt` index)                                                                                                                                                     | Default ascending `createdAt` indexes (relying on PostgreSQL backward scans)                                                    | Pagination loads newest-first with a `(createdAt, id)` cursor; a matching `DESC, DESC` index represents that contract explicitly and serves the ordering without depending on backward-scan behaviour.                                                                                                                                                                                                                                                                                                                                                   | Ascending range scans for the rare oldest-first case use the index less optimally; acceptable because the product only paginates newest-first.                                                                              |
| DB-level `VARCHAR(n)` caps mirroring `packages/shared/src/constants/limits.ts` (`displayName` 80, channel `name` 80 / `description` 250, message `content` 4000, reaction `emoji` 64) plus `VARCHAR(255)` on file `originalName`/`storedName`/`mimeType`                              | Unbounded `TEXT` everywhere, relying solely on Zod validation at the API                                                        | DB caps add defense-in-depth against oversized direct writes (DoS/data-integrity) independent of the API. The 80/250/4000/64 values mirror the shared limit constants; 255 is the conventional bound for filesystem names and MIME types. No `CHECK` constraint is added — the forbidden Message channel/DM XOR check is intentionally omitted.                                                                                                                                                                                                          | The DB caps must stay in sync with the shared constants if those limits change; the CP2 Prisma schema expresses these via `@db.VarChar(n)`.                                                                                 |
| `File.uploadedById` made nullable with FK `ON DELETE SET NULL ON UPDATE CASCADE`                                                                                                                                                                                                      | Keep `NOT NULL` with `ON DELETE CASCADE` (delete file metadata when the uploader is deleted)                                    | The checkpoint requires uploaded-file records to survive deletion of the uploader; `SET NULL` preserves the file row and clears only the author reference, which `CASCADE` cannot do on a non-nullable column.                                                                                                                                                                                                                                                                                                                                           | A file can exist with no uploader (`uploadedById IS NULL`); consumers and the CP2 Prisma relation must treat the uploader as optional.                                                                                      |
| `Message.contentTsv` kept as `Unsupported("tsvector")? @default(dbgenerated("to_tsvector('english'::regconfig, (content)::text)"))` in `schema.prisma`, with a marker comment that the real column is `GENERATED ALWAYS AS (...) STORED`, owned by raw-SQL migration `20260527000001` | Remove the `@default` and document the column as unmanaged; model it as a plain `tsvector` column                               | `prisma db pull` of the deployed STORED generated column reproduces exactly this `@default(dbgenerated(...))` form, and `migrate diff --from-migrations -> --to-schema-datamodel` reports NO drift against it; removing the `@default` INTRODUCES drift (`ALTER COLUMN ... DROP DEFAULT`), and a from-empty diff emits an invalid `DEFAULT to_tsvector(content)` (column ref in a DEFAULT). So `@default(dbgenerated())` is Prisma's own canonical introspection of a STORED generated column and is the drift-free choice (CP2 review remediation, C4). | `prisma migrate dev` must never regenerate this column from the schema (it would emit the invalid DEFAULT form); the generated-column DDL lives only in the raw-SQL migration, which is the source of truth for the column. |
| Every Prisma relation declares an explicit `onUpdate` (`Cascade` on all FKs except `Message.parent`, which is `onDelete: NoAction, onUpdate: NoAction`) mirroring the migration FK clauses verbatim                                                                                   | Rely on Prisma's implicit `onUpdate: Cascade` default and omit the annotations                                                  | The init migration emits `ON UPDATE CASCADE` on all 14 FKs except `Message_parentId_fkey` (`ON UPDATE NO ACTION`); stating each `onUpdate` explicitly makes `schema.prisma` self-documenting and proves zero drift — `migrate diff` confirms "No difference detected" because the explicit value equals Prisma's implicit default (CP2 review remediation, M29).                                                                                                                                                                                         | None of note; the explicit annotations equal the generated SQL, so behaviour is unchanged and the schema simply states what the migration already enforces.                                                                 |

## `packages/api` — Configuration (env loader)

| What was decided                                                                                                                                                         | What alternatives existed                                                                   | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                 | What risks it carries                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Zod-validated env loader (`packages/api/src/config/env.ts`) calls `EnvSchema.parse(process.env)` inside a `loadEnv()` function and throws a formatted `Error` on failure | `safeParse` + `process.exit(1)`; lazy per-getter validation; no validation at all           | Fail-fast at module load surfaces an immediate, human-readable diagnostic before any other module initialises, satisfying Gate 12; throwing rather than calling `process.exit` keeps the module import-safe and unit-testable while still aborting startup, and a `z.ZodError` is reformatted via `error.format()` into the thrown message. Updated during CP2 review remediation — the earlier `safeParse` + `process.exit(1)` mechanism is superseded. | An invalid environment cannot be recovered without a restart — the intended behaviour for a configuration error; the process entry point must not swallow the throw.                                                                                          |
| `JWT_SECRET` requires `.min(32)` characters (no default)                                                                                                                 | No minimum; 16 chars; 64 chars                                                              | 32 bytes is the recommended floor for HS256 signing-key strength (AAP §0.8.4 security baseline).                                                                                                                                                                                                                                                                                                                                                         | Operators must supply a real 32+ char secret; the `.env.example` placeholder is long enough to pass and documents the requirement.                                                                                                                            |
| `BCRYPT_ROUNDS` requires `.min(10)`, default `10`                                                                                                                        | No minimum; default/minimum of 12                                                           | 10 is the OWASP-recommended minimum bcrypt cost factor (AAP §0.8.4); keeping it as the default while allowing higher values lets ops tune the work factor via env.                                                                                                                                                                                                                                                                                       | Higher rounds increase login latency; ops can raise the value when hardware allows.                                                                                                                                                                           |
| `CORS_ORIGIN` defaults to `http://localhost:5173`                                                                                                                        | No default (require it); derive it from `VITE_APP_URL`                                      | Neither root nor `packages/api/.env.example` declares `CORS_ORIGIN`, so a default equal to the Vite dev origin keeps `make local` working out of the box (Gate 1).                                                                                                                                                                                                                                                                                       | Production deployments must override it; this row is the single source of truth for that obligation.                                                                                                                                                          |
| The env loader throws a formatted `Error` on failure and no longer calls `console.error`; Pino is the only logging path in the API package                               | Log the failure via `console.error` before exiting; a temporary console-based pseudo-logger | The Pino logger imports from env.ts, so logging the env failure there would be circular and the logger is not yet constructed; throwing defers presentation to the process entry point and keeps env.ts free of `console`. Updated during CP2 review remediation — the earlier `console.error` fail-fast use is superseded.                                                                                                                              | The thrown error must reach a top-level handler or Node's default printer to be visible; intended, since an unhandled configuration error should abort with a stack trace.                                                                                    |
| `z.coerce.number()` for the numeric vars `PORT`, `MAX_FILE_SIZE_MB`, `BCRYPT_ROUNDS`                                                                                     | A custom `transform`; `parseInt` + `refine`                                                 | `process.env` values are always strings; `z.coerce.number()` is the idiomatic Zod way to convert and then validate via `.int().positive()` / `.min()`.                                                                                                                                                                                                                                                                                                   | None of note.                                                                                                                                                                                                                                                 |
| `.startsWith('postgresql://')` on `DATABASE_URL` and `.startsWith('redis://')` on `REDIS_URL`, in addition to `.url()`                                                   | `z.string().url()` alone                                                                    | `.url()` accepts any scheme, so a mis-pasted `http://` connection string would pass; the scheme guard rejects that class of typo at startup with a clear message before Prisma/ioredis fail later.                                                                                                                                                                                                                                                       | Belt-and-suspenders validation that overlaps `.url()`; accepted in exchange for an earlier, clearer failure.                                                                                                                                                  |
| Schema field named `PORT` (reads `process.env.PORT`, default `3000`), matching the `env.PORT` export contract                                                            | Read `API_PORT` (the name used in both `.env.example` files); support both names            | The file schema and agent prompt specify `PORT`, and consumers (`index.ts` → `httpServer.listen(env.PORT)`) depend on `env.PORT`; the default `3000` converges with the `.env.example` `API_PORT=3000`, so `make local` (Gate 1) works with no `PORT` set.                                                                                                                                                                                               | If an operator sets a non-default `API_PORT` expecting the server to follow, `env.PORT` will not track it; the server port and the Makefile health-check URL should both align on `PORT` (or `PORT` should be set explicitly) when a custom port is required. |

## `packages/web` — Web client package manifest

| What was decided                                                                                                                                                          | What alternatives existed                                                                                    | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | What risks it carries                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Package named `@app/web` (workspace dependency declared as `@app/shared`)                                                                                                 | Use the AAP/agent-prompt illustrative name `@blitzy-slack/web` (and `@blitzy-slack/shared`)                  | The actual workspace convention set during setup is `@app/*` — `packages/shared`/`db`/`api` are `@app/shared`/`@app/db`/`@app/api`, and the root `dev:web` script, the `Makefile` `local`/`dev` targets, and `pnpm --filter` all reference `@app/web`. Naming the package `@blitzy-slack/web` would break `pnpm install` (no `@blitzy-slack/shared` package exists to satisfy `workspace:*`) and every `--filter @app/web` invocation. Mirrors the precedent already recorded for `@app/db`. | None functional; this aligns the manifest with the real workspace. If the monorepo is ever renamed to `@blitzy-slack/*`, every package, the root scripts, and the `Makefile` must be renamed together. |
| `@tailwindcss/postcss` is NOT declared as a dependency                                                                                                                    | Declare `@tailwindcss/postcss` (as the agent-prompt skeleton suggested, "referenced by `postcss.config.js`") | The committed `packages/web/postcss.config.js` is a deliberate no-op (`export default { plugins: {} }`) because Tailwind v4 is processed by the `@tailwindcss/vite` plugin wired in `vite.config.ts`; nothing imports `@tailwindcss/postcss`, and it is not listed in AAP §0.3.1. Declaring it would add an unused dependency.                                                                                                                                                               | If a future change moves Tailwind processing to the PostCSS pipeline, `@tailwindcss/postcss` must be added then; the `vite` plugin remains the single processing path until that change is made.       |
| Build/test tooling (`tailwindcss`, `@tailwindcss/vite`, `vite`, `@vitejs/plugin-react`, `@types/react`, `@types/react-dom`, `@playwright/test`) kept in `devDependencies` | Place the Tailwind packages in `dependencies` (per the agent-prompt skeleton)                                | These are build-time tools: Tailwind compiles to CSS at build, Vite bundles, types and Playwright are dev-only — none are imported into the runtime bundle. Keeping them in `devDependencies` matches the committed manifest and the sibling treatment of `vite`/`@vitejs/plugin-react`. For a private, never-published workspace app the placement is immaterial to resolution (pnpm installs both groups for workspace packages).                                                          | If this package were ever published or installed with `--prod`, build tooling would be excluded — acceptable because it is `private` and only ever installed in dev mode via `pnpm install`.           |
| `typescript` is NOT declared in this package's `devDependencies`                                                                                                          | Declare `typescript` locally (as the earlier committed manifest did)                                         | The root workspace declares `typescript` and pnpm hoists its `tsc` bin to the workspace-root `node_modules/.bin`, which is exposed to filtered package execution — verified: `pnpm --filter @app/api exec tsc` and `--filter @app/shared exec tsc` both resolve TypeScript 5.9.3 without a local declaration. Omitting it matches the `@app/shared`/`@app/db`/`@app/api` siblings, none of which declare `typescript`.                                                                       | If the root ever drops `typescript`, this package's `build`/`typecheck` (`tsc -p tsconfig.json --noEmit`) would fail; the root manifest is the single source for the compiler version.                 |
| `cmdk` and the `@radix-ui/react-accordion`/`@radix-ui/react-alert-dialog` primitives are retained as runtime `dependencies`                                               | Restrict the manifest to only the ten Radix packages explicitly enumerated in the agent-prompt skeleton      | `cmdk` backs the shadcn `Command` primitive that AAP §0.5.2 maps to the Ctrl+K quick switcher (the skeleton omitted it); `accordion`/`alert-dialog` back catalogued shadcn primitives from AAP §0.2.2 and are present in the committed manifest. AAP §0.3.1 declares `@radix-ui/react-*` as "latest matching shadcn add", so a superset that covers the catalogued primitives is compliant and avoids breaking components added later by `npx shadcn@latest add`.                            | A small number of Radix packages may go unused if the corresponding components are never generated; unused entries add negligible install weight and are not bundled unless imported.                  |
| `build` runs `tsc -p tsconfig.json --noEmit && vite build`; the dead `test:e2e` script was removed                                                                        | `build` = `vite build` alone; keep a per-package `test:e2e` script                                           | Vite/esbuild does not type-check, so prefixing `tsc --noEmit` enforces Rule 3 (strict, zero-warning) before bundling in the root `pnpm -r build` path. The former `test:e2e` ("playwright test") was dead because there is no `playwright.config` in `packages/web` — E2E runs from the repository root (`make test-e2e` → root `playwright test` over `packages/web/test/e2e`).                                                                                                             | The `tsc` gate makes `build` fail on any type error (intended). E2E is now invoked only from the root; a developer must not expect `pnpm --filter @app/web test:e2e` to exist.                         |
| `vite.config.ts` defines no `server.proxy`; the web client calls the API at the absolute `import.meta.env.VITE_API_URL` / `VITE_WS_URL`                                   | Add a Vite dev proxy that forwards `/api` and the Socket.io path to `http://localhost:3000`                  | AAP §0.6.2 places CORS on the API side (the API allow-lists the `http://localhost:5173` dev origin — see the `CORS_ORIGIN` row above), so the browser reaches the API directly on its own origin. A proxy would mask CORS misconfiguration in development and make the effective request origin ambiguous between dev (same-origin, proxied) and production (cross-origin).                                                                                                                  | The API must keep the Vite origin on its CORS allow-list (already defaulted). A future single-origin deployment would still work via absolute URLs but without the proxy's same-origin convenience.    |
| `server.port: 5173` and `preview.port: 4173` are pinned with `strictPort: true`                                                                                           | Omit `strictPort` and let Vite auto-increment to the next free port when 5173/4173 is occupied               | The dev port is contractually referenced by `VITE_APP_URL`, the root `playwright.config.ts` `baseURL`, and the API CORS allow-list; a silent shift to 5174 would break the Playwright `baseURL` and CORS. `strictPort` keeps `make local` deterministic by failing loudly instead of drifting to another port.                                                                                                                                                                               | If 5173 (or 4173 for `vite preview`) is already in use, the server refuses to start — an intentional fail-fast that requires the developer to free the port before retrying.                           |
| `vite.config.ts` sets `build.target: 'es2022'`                                                                                                                            | Vite's default `'modules'` target; a lower `'es2015'` or higher `'esnext'`                                   | `es2022` matches `tsconfig.base.json` `target: ES2022`, so the bundler emits the language level TypeScript type-checks against (top-level `await`, class fields), keeping build output and type-check assumptions aligned; the PoC targets current Chromium/Firefox/WebKit, all of which support ES2022 (CP2 review remediation moved this rationale out of the config comment, M17).                                                                                                        | Pre-2022 browsers are unsupported; acceptable for the PoC's modern-browser scope.                                                                                                                      |

## `packages/web` — shadcn/ui component manifest (`components.json`)

| What was decided                                                                                                                        | What alternatives existed                                                                                                                                                    | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | What risks it carries                                                                                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| shadcn `style: "new-york"` selected in `components.json`                                                                                | `style: "default"`, the other shadcn preset                                                                                                                                  | shadcn is deprecating the `default` style and initialises all new Tailwind-v4 projects with `new-york`; its denser shadows and smaller default radius better match Slack's tight, compact web UI (Rule 1), and it keeps the manifest aligned with the CLI's current defaults rather than a deprecated preset.                                                                                                                                                                                          | `style` cannot be changed after initialisation without deleting and re-adding components; a later switch to `default` would require regenerating every `src/components/ui/*` primitive.                         |
| `tailwind.config` left as an empty string (`""`) — a deviation from the agent-prompt skeleton, which illustrated `"tailwind.config.ts"` | Set `"config": "tailwind.config.ts"` and author a `tailwind.config.ts` (the Tailwind v3 / Next.js convention)                                                                | The web package uses Tailwind CSS v4, processed by the `@tailwindcss/vite` plugin with a deliberate no-op `postcss.config.js`; there is no `tailwind.config.ts` and theming is expressed via the `@theme` directive in `src/index.css` (AAP §0.5.1, §0.5.3). The official shadcn documentation states that for Tailwind v4 the `tailwind.config` field must be left blank; pointing it at a non-existent file would misconfigure the CLI. The skeleton value reflected the older Tailwind-v3 template. | If the project is ever downgraded to Tailwind v3, or a JS/TS Tailwind config is introduced, this field must then point at that config's path; until then the empty string is the correct, CLI-supported value.  |
| `baseColor: "neutral"` with `cssVariables: true` (and `prefix: ""`) chosen as the manifest theming foundation                           | Another shadcn base palette (`slate` / `gray` / `zinc` / `stone`); `cssVariables: false` (inline colour utilities); or baking the Slack aubergine directly into the manifest | `neutral` is a pure-grayscale foundation onto which the Slack-aubergine palette is layered via `@theme` overrides in `src/index.css` (AAP §0.5.3), which keeps shadcn updates clean and the colour override in a single place. `cssVariables: true` is required by Tailwind v4's `@theme` token mechanism; `false` would emit inline colour utilities and break the design-token architecture. The manifest itself carries no colour literals, consistent with Rule 1 and Rule 3.                      | `baseColor` and `cssVariables` cannot be changed after initialisation without deleting and re-installing components; the aubergine override must be maintained in `src/index.css` rather than in this manifest. |

## `packages/web` — Theme tokens (`src/index.css`)

| What was decided                                                                                                                        | What alternatives existed                                                                                                                       | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | What risks it carries                                                                                                                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OKLCH color space for every theme token in the `@theme` block                                                                           | sRGB hex literals, HSL, or P3 wide-gamut                                                                                                        | Tailwind v4's default palette is authored in OKLCH and the `@theme` mechanism expects static color values; OKLCH is perceptually uniform, so the aubergine and lavender steps interpolate predictably, and Rule 1 + Rule 3 forbid hex/rgb/hsl literals in app code.                                                                                                                                                                                                                                  | OKLCH requires a modern engine (no IE11); acceptable because the PoC targets current Chromium/Firefox/WebKit.                                                                            |
| Slack-aubergine sidebar palette expressed as the eight `--color-sidebar*` `@theme` tokens that override shadcn's neutral baseColor      | Keep shadcn's grayscale sidebar; or bake the aubergine directly into each component                                                             | Rule 1 (Screenshot-Driven UI) requires the dark-aubergine sidebar and lavender active accent seen in screenshots 29/100; centralising them as tokens keeps every shadcn primitive and utility class consistent and the override in one place (AAP §0.5.3).                                                                                                                                                                                                                                           | The OKLCH values are snapped approximations of sampled sRGB; if Slack's palette is re-captured the tokens must be re-tuned.                                                              |
| `--color-sidebar-bg` anchored to the canonical Slack aubergine `#3F0E40` rather than the lighter sampled channel-sidebar surface        | Pin `--color-sidebar-bg` to the sampled channel-sidebar gradient midpoint (~`oklch(0.36 0.087 323)`, sRGB `#4D2651`–`#562F5C`)                  | AAP §0.5.3 explicitly cites "~#3F0E40" as the sidebar reference and it is Slack's canonical brand aubergine; the far-left workspace rail samples at `#3A0E3C`–`#3F1543`, confirming this exact aubergine is present in the screenshots. The lighter channel-sidebar step is `--color-sidebar-hover`. The exact AAP §0.5.3 token names `--color-sidebar-bg` and `--color-sidebar-hover` are used — CP2 review remediation renamed them from the earlier `--color-sidebar` / `--color-sidebar-accent`. | The rendered channel-sidebar column is ~10–15% darker than the Mobbin capture; still unmistakably Slack aubergine, and Gate 8 visual thresholds tolerate the difference.                 |
| `--color-primary` set to the Slack blue/link accent `#1164A3` (`oklch(0.491 0.125 248)`), replacing shadcn neutral's near-black primary | Keep shadcn neutral primary (`oklch(0.205 0 0)`); or use Slack's green send-button CTA `#007A5A`                                                | AAP §0.5.3 maps the active-channel/link accent (~#1164A3) to `--color-primary`; screenshots 29/100 show links (`#design`, Edit description) and the reaction chip rendered in this blue.                                                                                                                                                                                                                                                                                                             | Slack's send-button green is a separate component-level accent and is intentionally NOT modelled as `--color-primary`; downstream agents must not treat `bg-primary` as the send button. |
| Light mode only — no dark-mode token set in this file                                                                                   | Add a parallel `.dark` / `:root.dark` token block with dark OKLCH values                                                                        | Dark mode is out of scope for the PoC (AAP §0.7.2) and the reference screenshots depict the light content area; the file-spec restricts this file to one `@theme` and one `@layer base` block.                                                                                                                                                                                                                                                                                                       | A future dark mode requires a second token set plus a `dark` variant and runtime variable indirection; recorded here as a known follow-up.                                               |
| Tailwind v4 `@import 'tailwindcss';` + `@theme` directive, not the v3 `@tailwind base/components/utilities` directives                  | Tailwind v3 directives with a JS `theme` config object                                                                                          | AAP §0.5.1 prescribes Tailwind v4 with `@tailwindcss/vite`; the `@theme` directive emits both the CSS variable and the matching `bg-*`/`text-*`/`border-*` utility for each token, which `components.json` `cssVariables: true` relies on.                                                                                                                                                                                                                                                           | v4 is newer and a few third-party plugins lag it; none are used by this file.                                                                                                            |
| Each `--color-*` custom property declared exactly once (the Slack primary is written directly, not as a neutral value then re-declared) | Emit shadcn's neutral `--color-primary` and then re-declare the Slack value lower in the same block (as the file-spec illustrated the override) | A single declaration is unambiguous about the final value and avoids any duplicate-custom-property diagnostic if stylelint is added later (Rule 3 zero-warning).                                                                                                                                                                                                                                                                                                                                     | The override relationship to shadcn neutral is documented here rather than being visible as two declarations in the file.                                                                |
| `@import 'tailwindcss';` written with single quotes                                                                                     | Double quotes (`@import "tailwindcss";`) as the agent file-spec illustrated                                                                     | The repository's enforced Prettier config sets `singleQuote: true`, and `prettier --check .` (run by `make format` and the format gate) rewrites CSS strings and attribute selectors to single quotes; single- and double-quoted `@import` are equivalent to the Tailwind v4 parser, so honouring the formatter keeps the file passing the build's format gate.                                                                                                                                      | Purely stylistic; if the `singleQuote` policy ever changes the formatter rewrites it automatically.                                                                                      |
| The active-channel selected-state highlight (light lavender `#F7EDFE`, ~`oklch(0.959 0.025 312)`) is NOT added as a theme token         | Add a bespoke `--color-sidebar-selected` token                                                                                                  | The shadcn sidebar token contract is fixed at the eight `--color-sidebar*` names and the file-spec mandates exactly those; the selected-channel pill is a component-level treatment owned by the downstream ChannelList component.                                                                                                                                                                                                                                                                   | The reference value is recorded here so the downstream component can reuse it; a token can be promoted later if several components need it.                                              |

### Color token resolution

All sRGB → OKLCH conversions use the standard sRGB → linear-light → OKLab → OKLCH pipeline (Ottosson, 2020). Sampled sRGB values are averaged pixel patches taken from `screenshots/Slack web Jul 2024 29.png` and `screenshots/Slack web Jul 2024 100.png`. The shadcn neutral baseColor tokens are preserved verbatim from the shadcn "neutral" light-mode palette and are not re-derived here.

| Token(s)                                                                              | sRGB reference (source)                                                                                 | Final OKLCH                    | Note                                                            |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------------ | --------------------------------------------------------------- |
| `--color-sidebar-bg`                                                                  | `#3F0E40` (AAP §0.5.3 canonical aubergine; workspace rail sampled `#3A0E3C`–`#3F1543`)                  | `oklch(0.272 0.101 327)`       | Slack brand aubergine; the dominant sidebar surface.            |
| `--color-sidebar-hover`                                                               | ~`#532C58` (channel-sidebar lighter step, sampled `#4D2651`–`#562F5C`)                                  | `oklch(0.37 0.09 323)`         | Hover / lighter-aubergine step.                                 |
| `--color-sidebar-border`                                                              | ~`#310A33` (darker than the sidebar background)                                                         | `oklch(0.234 0.086 326)`       | Derived darker aubergine for dividers.                          |
| `--color-sidebar-ring`                                                                | ~`#8B5C90`                                                                                              | `oklch(0.545 0.096 323)`       | Mid-lavender focus ring.                                        |
| `--color-sidebar-foreground`                                                          | `#F8DAFF` (lightest sampled sidebar text, chroma reduced)                                               | `oklch(0.94 0.022 320)`        | Near-white lavender, tuned for legibility on aubergine.         |
| `--color-primary`, `--color-sidebar-primary`                                          | `#1164A3` (AAP §0.5.3 lavender/blue accent; link samples `#305C91`/`#356592`/`#38649B` confirm the hue) | `oklch(0.491 0.125 248)`       | Active-channel / link / focus accent.                           |
| `--color-primary-foreground`, `--color-sidebar-*-foreground`                          | near-white                                                                                              | `oklch(0.985 0 0)`             | Foreground on the accent/aubergine surfaces (shadcn neutral).   |
| `--color-background`, `--color-card`, `--color-popover`                               | `#FFFFFF` (white content area, screenshot 29)                                                           | `oklch(1 0 0)`                 | shadcn neutral; the white content column.                       |
| neutral foreground / secondary / muted / accent / destructive / border / input / ring | shadcn neutral light-mode (grayscale; destructive in the red family)                                    | as authored in `src/index.css` | Preserved verbatim from the shadcn "neutral" baseColor palette. |

## `scripts` — Test-user seed (`seed-via-api.ts`)

| What was decided                                                                                                                                                                                          | What alternatives existed                                                                               | Why this choice was made                                                                                                                                                                                                                                                                                                                     | What risks it carries                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The seeder is **dual-mode**: a CLI entry (detected via `process.argv[1] === fileURLToPath(import.meta.url)`, used by `make seed`) and a default-exported `globalSetup` consumed by `playwright.config.ts` | Two separate scripts (one CLI, one for Playwright); a Playwright-only or CLI-only seeder                | One module is the single source of the seed logic for both `make seed` and the E2E run, so the test user is created identically in both paths with no drift                                                                                                                                                                                  | The dual-entry detection relies on `import.meta.url`/`argv[1]` semantics under `tsx` ESM; documented here so future refactors keep both entry points working        |
| Seeding calls `POST /api/auth/register` with Node's **native `fetch`** and **no workspace imports**                                                                                                       | `node-fetch` or `axios`; importing `@app/shared` for the request type; inserting into Postgres directly | Rule 4 (AAP §0.8.1) mandates seeding through the registration endpoint, never a direct DB insert; Node 22's built-in `fetch` removes a dependency, and keeping the script import-free keeps `tsx` ESM resolution trivial                                                                                                                     | The hardcoded request body must stay shape-compatible with `registerSchema` in `packages/shared/src/schemas/auth.ts`; if that schema changes, this body must follow |
| HTTP **201 and 409 are both treated as success** (idempotent seeding); any other status, a network error, or the timeout is a failure                                                                     | Treat only 201 as success (re-runs fail once the user exists); ignore all errors                        | `make local` and the E2E setup may run repeatedly against a populated database; a 409 `Conflict` (user already exists) means the seed goal is already met, so it counts as success                                                                                                                                                           | A 409 from an unrelated cause would be masked as success; acceptable because the register endpoint returns 409 only for the duplicate-email case                    |
| A **60-second `AbortController` timeout** guards the request                                                                                                                                              | No timeout (hang until TCP fails); a shorter 5–10s timeout                                              | `make local` starts the API in the background and polls health, but the seed can still race a slow first boot; 60s tolerates cold starts while still failing rather than hanging forever                                                                                                                                                     | An API that takes longer than 60s to first response fails the seed; the error message tells the developer to confirm the API is running and reachable               |
| The seeder polls `GET /api/health` (`waitForApiHealth`) until it returns 2xx before calling `POST /api/auth/register`, in both the CLI and Playwright-globalSetup paths                                   | Register immediately and rely on the `Makefile` health gate alone; gate only the `make local` path      | Standalone `make seed` and the Playwright `globalSetup` can run without the `Makefile` loop in front of them, so gating inside the script guarantees Rule 4's "seed only after the API is healthy" for every invocation (CP2 review remediation, M20/M21); each poll is bounded by its own `AbortController` and the loop by a 60s deadline. | If `/api/health` never reaches 2xx within 60s the seed fails with a descriptive error; intended fail-fast rather than registering against an unready API.           |

## `packages/api` — Service errors (`middleware/errors.ts`)

| What was decided                                                                                                                                                                                                                                                                                                                                                      | What alternatives existed                                                                                                      | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | What risks it carries                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Services throw **typed error classes** from `middleware/errors.ts` (`abstract ServiceError` → `UnauthorizedError` 401 / `ForbiddenError` 403 / `NotFoundError` 404 / `ConflictError` 409 / `ValidationError` 400 / `AppError` caller-supplied); each subclass exposes a `readonly statusCode` that the error-handler middleware reads, with `instanceof` for dispatch | Services return HTTP responses directly (`res.status().json()`); framework-specific error objects; a single untyped error type | Keeping services transport-agnostic (they never touch `res`) lets the same service code back both HTTP routes and Socket.io handlers, and centralises status mapping in one place; carrying `statusCode` on each subclass makes the HTTP status intrinsic to the error rather than re-derived by a `switch`. The module lives under `middleware/` (CP2 review remediation moved it from `services/errors.ts`) so the AAP/checkpoint import path `packages/api/src/middleware/errors.ts` resolves. | `instanceof` identity must survive module boundaries — the base restores the prototype chain with `Object.setPrototypeOf` so the checks stay reliable under NodeNext; a forgotten prototype restore would break dispatch. |
| `errors.ts` is a **zero-import, lowest-level module**, and Prisma's own `PrismaClientKnownRequestError` is intentionally NOT wrapped here (the error-handler maps it separately via `P2002 → 409`, `P2025 → 404`)                                                                                                                                                     | Wrap Prisma errors into the custom classes inside `errors.ts`; import Prisma types here                                        | Zero imports keep the module at the bottom of the services dependency tree so every service and the middleware can import it without cycles; Prisma error mapping belongs to the middleware that already owns HTTP translation                                                                                                                                                                                                                                                                    | New Prisma error codes are mapped in the error-handler middleware, not here — a separation contributors must remember                                                                                                     |

## `packages/api` — Socket.io room keys (`sockets/rooms.ts`)

| What was decided                                                                                                                                                                                                                                         | What alternatives existed                                                            | Why this choice was made                                                                                                                                                                                               | What risks it carries                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Room keys are built by **four centralised pure helpers** — `channelRoom` → `channel:<id>`, `dmRoom` → `dm:<id>`, `threadRoom` → `thread:<id>`, `userRoom` → `user:<id>` — and every `io.to`/`io.in` and `socket.join`/`socket.leave` routes through them | Inline string concatenation at each call site; an enum or namespace of room prefixes | A single source for the room-key format guarantees the keys the `@socket.io/redis-adapter` publishes on are identical everywhere (AAP §0.4.5); inline concatenation drifts and silently breaks cross-instance delivery | The helpers do no input validation (cuid shape is enforced by Zod at the route/socket boundary); passing an unvalidated id would produce a malformed room key, so callers must validate first |

## `packages/api` — Jest runner config (`jest.config.ts`)

| What was decided                                                                                                                                                                                                                                                                     | What alternatives existed                                                                                                  | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                      | What risks it carries                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The Jest config lives at the package root `packages/api/jest.config.ts` with `rootDir: '.'` (`packages/api/`)                                                                                                                                                                        | A config co-located with the suites under `test/jest.config.ts` (`rootDir: '..'`)                                          | The package-root location is the AAP/checkpoint-conventional path a bare `jest` invocation auto-discovers, so `make test` (script-driven) and a bare `jest` behave identically; CP2 review remediation moved it here from `test/jest.config.ts` and dropped the `--config` flag from the package scripts                                                                                                                      | None of note; standard discovery means no `--config` flag is required, and `test/setup.ts` is wired via `setupFiles`                                                         |
| ts-jest with the `ts-jest/presets/default-esm` preset is the test transformer                                                                                                                                                                                                        | `babel-jest` (no type-checking), `@swc/jest` (fastest, no type-checking), or Bun's test runner (different runtime)         | AAP §0.3.1 pins `ts-jest`; running the suites through ts-jest type-checks every `.ts` file at compile time (`diagnostics.warnOnly: false`), so the test run itself enforces Rule 3 strictness. The `default-esm` preset (not `default`) matches the api package's `"type": "module"`                                                                                                                                          | ts-jest is slower than swc/esbuild transformers; acceptable at PoC suite size and offset by the type-safety guarantee                                                        |
| Workspace dependencies are resolved by `moduleNameMapper` straight to each package's **source** (`<rootDir>/../shared/src`, `<rootDir>/../db/src`) rather than to built `dist/` output                                                                                               | Build every workspace package to `dist/` before testing; rely solely on the pnpm symlink plus each package's `exports` map | Source resolution removes a build step from `make test`, lets ts-jest compile workspace TypeScript directly, and mirrors the source-first dev model Vite uses in `packages/web`                                                                                                                                                                                                                                               | Tests can run against source that has not been built to `dist/`; mitigated because each package has its own `tsc -p … --noEmit` typecheck script                             |
| The `moduleNameMapper` keys target the real workspace names **`@app/shared`** and **`@app/db`**                                                                                                                                                                                      | The `@blitzy-slack/shared` / `@blitzy-slack/db` names written in this file's own implementation seed                       | Deviation logged per the Explainability rule: the packages are actually scoped `@app/*` (their `package.json` `name` fields and the api package's `workspace:*` deps confirm it). The seed's `@blitzy-slack/*` keys would have produced a mapper that matches no import — dead config. This aligns with the existing log rows that scope packages `@app/*`                                                                    | If the workspace is ever rescoped to a different npm scope, these mapper keys must be updated in lockstep                                                                    |
| `ts-node@^10.9.2` was added to `packages/api` devDependencies, beyond the AAP §0.3.1 inventory                                                                                                                                                                                       | Keep the loader-free root `jest.config.mjs`; precompile the config to JS; author a `.cjs`/`.js` config instead of `.ts`    | Deviation logged per the Explainability rule: Jest 29 reads a **`.ts`** config by registering `ts-node` at bootstrap (ts-jest transforms test files at runtime but does not read the config file itself). The assigned file is mandated to be `.ts`, so `ts-node` is a necessary implicit dependency (AAP §0.1.2). Jest invokes it with a forced `module: CommonJS` override, so the ESM-package `.ts` config loads correctly | One extra devDependency plus a `pnpm-lock.yaml` update; the lockfile delta is confined to the ts-node subtree and `--frozen-lockfile` was re-verified so Gate 1 still passes |
| The api `test` / `test:watch` / `test:coverage` scripts rely on Jest auto-discovery of the package-root `jest.config.ts` (no `--config` flag); the redundant `jest.config.mjs` stub was deleted and `--passWithNoTests` was removed so the coverage gate cannot pass with zero tests | Keep an explicit `--config` flag; leave the `.mjs` stub in place; retain `--passWithNoTests`                               | A single canonical root config with auto-discovery makes `make test` and a bare `jest` behave identically; removing `--passWithNoTests` (CP2 review remediation) restores the Gate 13 >=80% coverage gate now that a real `errors.test.ts` suite exists                                                                                                                                                                       | A developer must keep at least one runnable test in the package or `jest` exits non-zero (intended for the coverage gate)                                                    |
| Coverage thresholds set to **80** for lines/functions/statements and **70** for branches (`coverageThreshold.global`), collected from `src/services/**/*.ts`                                                                                                                         | A uniform 80 across all four metrics; a lower 60 floor; no enforced threshold                                              | Gate 13 mandates an 80% line floor on the services layer; lines/functions/statements reach 80 with reasonable suites, while branch coverage lags initially because error/ACL branches accrue more slowly, so a 70 branch floor lets the suite land without masking the line-coverage gate                                                                                                                                     | Branch coverage sits below the 80% industry norm until the suites mature; the branch floor can be raised once per-service ACL/error-path tests exist                         |

## `packages/api` — Configuration (Pino logger, `config/logger.ts`)

| What was decided                                                                                                                                                                                                                                                                                                                                                                                                                                      | What alternatives existed                                                                                                           | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                             | What risks it carries                                                                                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pino as the logging library, exposed as a single `logger` singleton constructed at module load                                                                                                                                                                                                                                                                                                                                                        | Winston; Bunyan; bare `console`                                                                                                     | Pino is the fastest structured logger, is JSON-first, is widely supported by aggregators (Loki, CloudWatch, Datadog), and has the `pino-http` / `pino-pretty` ecosystem the rest of the API relies on (Gate 10)                                                                                                                                                                                                                                      | Less feature-rich than Winston (no built-in transport fan-out) — acceptable for a PoC; a second sink would need a custom transport                                                    |
| `pino-pretty` transport is attached **only** when `NODE_ENV === 'development'`; test and production emit raw JSON to stdout                                                                                                                                                                                                                                                                                                                           | Pretty output in every environment; never pretty (raw JSON even in dev)                                                             | Production logs must stay machine-ingestible JSON, while dev ergonomics need human-readable colorized lines; gating on `NODE_ENV` gives both without a code change                                                                                                                                                                                                                                                                                   | The dev pretty path spawns a `pino-pretty` worker thread on first flush (negligible overhead); the two code paths must be kept behaviourally equivalent apart from formatting         |
| Redaction policy covers authorization (`req.headers.authorization`, `res.headers.authorization`, bare `authorization`, `*.authorization`), cookies (`req.headers.cookie`, `req.headers["set-cookie"]`, `res.headers["set-cookie"]`, `["set-cookie"]`) and credential/token fields (`password`, `passwordHash`, `token`, `secret`, `jwt` as bare keys plus `req.body.*` and one-level `*.` wildcards), with `censor: '[REDACTED]'` and `remove: false` | Redact nothing (trust producers); redact an exhaustive deny-list of everything that looks sensitive                                 | A conservative, explicit allow-list blocks the known secret-bearing fields (JWTs, cookies, set-cookie response headers, credentials) emitted by `pino-http` and ad-hoc logs while keeping the log shape intact for ingestion (`remove: false`). Expanded during CP2 review remediation to add bare/nested `authorization`, `set-cookie`, `secret`, and `jwt` variants.                                                                               | A novel secret-bearing field name would slip through; mitigated by this documented policy and by producers not passing secrets explicitly.                                            |
| Timestamps use `pino.stdTimeFunctions.isoTime` (ISO 8601 UTC strings)                                                                                                                                                                                                                                                                                                                                                                                 | Pino default Unix-epoch milliseconds; locale-specific formatting                                                                    | ISO 8601 is sortable, timezone-explicit, and the lingua franca of log aggregators                                                                                                                                                                                                                                                                                                                                                                    | Marginally larger log lines than an epoch integer (negligible)                                                                                                                        |
| The level formatter emits the string label (`formatters.level(label) => { level: label }`) so lines carry `"level":"info"`                                                                                                                                                                                                                                                                                                                            | Pino default numeric levels (`"level":30`)                                                                                          | String labels are human-scannable in a raw `tail` and friendlier to downstream parsing/alerting while preserving structured semantics                                                                                                                                                                                                                                                                                                                | Differs from the Pino default; documented here so consumers expect labels, not numbers                                                                                                |
| `env.LOG_LEVEL` (when set) overrides the `NODE_ENV`-derived default (`debug` in development, `info` in test/production), resolved in `resolveLogLevel()`                                                                                                                                                                                                                                                                                              | A `NODE_ENV`-only level with no override hook                                                                                       | Ops can raise or lower verbosity at runtime via env without a code change or redeploy                                                                                                                                                                                                                                                                                                                                                                | A forgotten `LOG_LEVEL=trace` (or `silent`) in production would over- or under-log; mitigated by env review and the fail-fast env loader making the value visible                     |
| `resolveLogLevel()` is annotated `: Level \| 'silent'` instead of the agent prompt's literal `: Level`                                                                                                                                                                                                                                                                                                                                                | Return pino's `Level` verbatim (the prompt's literal); treat `'silent'` as the `NODE_ENV` default and drop it; suppress with a cast | Deviation logged per the Explainability rule: pino 9.x's exported `Level` union omits `'silent'`, but `env.LOG_LEVEL` is validated as `z.enum([...,'silent']).optional()` and may legitimately be `'silent'`. `Level \| 'silent'` is structurally identical to pino's `LevelWithSilent`, is assignable to `LoggerOptions.level` (`LevelWithSilentOrString`), preserves the explicit-`silent` behaviour, and compiles with zero `@ts-ignore` (Rule 3) | If a future pino major adds `'silent'` to `Level`, the union becomes redundant (harmless); the narrowed name `Level` is still imported and used so the schema's import contract holds |
| The static `base.service` log field is the literal `'@blitzy-slack/api'` even though the workspace package is scoped `@app/api`                                                                                                                                                                                                                                                                                                                       | Use the real package name `@app/api`; derive it from `package.json` `name` at runtime                                               | Deviation logged per the Explainability rule: `service` is a free-form observability label (not a module specifier, so unlike the Jest `moduleNameMapper` keys it has no resolution requirement), the project itself is named `blitzy-slack` (catalog-info.yaml / mkdocs), and the file schema plus the spec's own validation command assert on exactly `"service":"@blitzy-slack/api"`                                                              | A reader correlating logs with the npm package name sees `@blitzy-slack/api` rather than `@app/api`; this row is the single source of truth reconciling the two                       |

## `packages/api` — Request validation middleware (`middleware/validate.ts`)

| What was decided                                                                                                                                                                                                         | What alternatives existed                                                                                                    | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | What risks it carries                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Validated payloads are funnelled through an explicitly `unknown`-typed local (`const data: unknown = parsed.data; req.body = data;` and `const source: unknown = req[target];`) before being written back to the request | Make `validate` generic over `z.infer<T>`; cast `parsed.data` to a concrete type; relax the `no-unsafe-assignment` lint rule | Zod's `ZodTypeAny.safeParse().data` and Express 5's `req.body`/`req[target]` are both typed `any`, which tripped `@typescript-eslint/no-unsafe-assignment` and failed the module-wide zero-warning gate (Rule 3). Routing the value through `unknown` is the lint-sanctioned way to tame an `any`, keeps the public overload signatures unchanged, and preserves runtime behaviour exactly. Fixed during `logger.ts` validation as the module-level final validator (the file is an in-scope AAP CREATE and had no call sites yet, so the change is zero-blast-radius) | The middleware still does not statically type the parsed output for downstream handlers; a future generic refactor over `z.infer<T>` would carry the schema's type through to `req.body`, and this row should be superseded when that lands |
| On Zod failure the validator calls `next(new ValidationError(...))` (built by `toValidationError(error, source)` carrying `error.flatten().fieldErrors`), not `next(parsed.error)` with the raw `ZodError`               | Pass the raw `ZodError` to `next`; format the 400 response inline in the middleware                                          | Wrapping in the project `ValidationError` (status 400) routes Zod failures through the same typed-error -> error-handler mapping as service-thrown errors, so HTTP and Socket.io paths produce one consistent 400 shape with field details (CP2 review remediation, M4); the raw `ZodError` would force the error-handler to special-case Zod.                                                                                                                                                                                                                         | `ValidationError.details` carries Zod's `fieldErrors` map; the error-handler must serialise it into the 400 body, and a future schema change reshapes those details.                                                                        |

## CP2 review remediation — seed decisions & dependency hardening

The eight decisions the AAP §0.6.3 seed mandates, recorded here during CP2 review remediation alongside the security upgrades they motivated.

| What was decided                                                                                                                                                        | What alternatives existed                                                                                       | Why this choice was made                                                                                                                                                                                                                                                                                                                                                              | What risks it carries                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| shadcn/ui consumed via the copy-paste CLI model (`npx shadcn@latest add`) that emits owned source under `src/components/ui/`, not as a runtime npm component dependency | Install a packaged component library (a published npm `@shadcn/ui`); build bespoke primitives directly on Radix | shadcn's distribution model IS copy-paste (AAP §0.5.1): the components become first-party source the project owns and restyles with the Slack-aubergine tokens, with no version-locked black-box dependency; it is the only supported shadcn workflow for Tailwind v4.                                                                                                                | Future shadcn upgrades require re-running the CLI per component rather than one `npm update`, and local edits can diverge from upstream; the `ui/**` tree is intentionally ESLint-ignored as vendored code. |
| Password hashing uses `bcryptjs` (pure JS), replacing native `bcrypt`                                                                                                   | Keep native `bcrypt@5.1.1`; switch to `argon2`; pin a patched `bcrypt`                                          | `bcrypt@5.1.1` pulls `@mapbox/node-pre-gyp -> tar@6.2.1`, which `pnpm audit` flags high-severity; `bcryptjs` is the AAP §0.3.2-documented portable fallback, has zero native/transitive deps (clearing the `tar` path), ships its own types, and exposes the same `hash`/`compare` API (CP2 review remediation, C3). No API code imported `bcrypt` yet, so the swap is manifest-only. | `bcryptjs` is ~30% slower than native `bcrypt` at the same cost factor; acceptable at PoC login volume and `BCRYPT_ROUNDS` is tunable.                                                                      |
| Client state (`auth`, `presence`) held in Zustand stores                                                                                                                | React Context + `useReducer`; Redux Toolkit; Jotai/Recoil                                                       | Zustand (AAP §0.3.1) gives a minimal hook store with selector subscriptions and no provider boilerplate, so a Socket.io event can update the presence map outside React and only subscribed components rerender; Context rerenders all consumers and Redux Toolkit adds boilerplate disproportionate to a PoC.                                                                        | Global singleton stores need discipline to avoid scattered mutations; mitigated by keeping mutations behind named actions in `stores/*.ts`.                                                                 |
| Routing uses React Router 7                                                                                                                                             | TanStack Router; Next.js App Router; wouter                                                                     | React Router 7 is the AAP §0.3.1 pin, is React 19-compatible, and its `BrowserRouter`/`createBrowserRouter` model covers the nested authenticated shell and modal/sheet routes without adopting a meta-framework; TanStack Router's type-safe routing adds a learning curve and codegen step not needed here.                                                                         | RR7 merged the former Remix data APIs; the project uses library (BrowserRouter) mode only and must not be confused with framework mode.                                                                     |
| Full-text search uses a PostgreSQL `tsvector` generated column + GIN index, queried via `prisma.$queryRaw`                                                              | `pg_trgm` trigram similarity; an external engine (Elasticsearch/Meilisearch/Typesense); `ILIKE` scans           | A native `tsvector`/GIN index gives ranked, language-aware search inside the existing Postgres with no extra infrastructure (AAP §0.4.4), keeping `make local` single-stack; trigram suits fuzzy/prefix not word search, and an external engine adds a service to run and sync — both disproportionate for the PoC.                                                                   | `tsvector` is word/stemming based (no fuzzy typo tolerance); ACL filtering runs in the service layer after the index match and the search service must always apply it.                                     |
| Cross-instance Socket.io fan-out uses `@socket.io/redis-adapter`                                                                                                        | A hand-rolled Redis pub/sub bridge over `io.emit`; a single sticky-session instance with no adapter; NATS/Kafka | The official adapter (Rule 2, AAP §0.3.1) transparently makes `io.to(room).emit(...)` reach clients on any instance via Redis pub/sub, handling room/ack semantics a custom bridge would have to reimplement; a single instance would not meet the horizontal-scale intent.                                                                                                           | Adds a hard Redis dependency for real-time delivery (already required for presence); an adapter major upgrade must track the Socket.io server version.                                                      |
| File uploads use `multer` 2.x (`^2.0.1`), upgraded from the `1.4.5-lts.x` line                                                                                          | Stay on `multer@1.4.5-lts.1`; use `busboy` directly; `formidable`                                               | The `1.4.4-lts.1 ... <2.0.1` range carries high-severity malformed-multipart and empty-field-name DoS advisories; `multer` 2.x patches both, keeps the identical `diskStorage`/`.single()` API (verified against `upload.ts`), and drops the `node-pre-gyp`/`tar` transitive path (CP2 review remediation, C2). 2.x is verified compatible with Express 5.                            | `multer` 2.x is a newer major; the middleware was smoke-tested but route-level upload tests land in CP3 and must re-confirm behaviour.                                                                      |
| Uploaded files stored on the local filesystem at `FILE_UPLOAD_PATH`, served via Express static                                                                          | Object storage (S3/GCS/MinIO) with signed URLs                                                                  | Local disk is the AAP §0.7.2-scoped PoC choice: no cloud credentials or extra service, keeping `make local` self-contained; object storage is explicitly an out-of-scope production follow-up.                                                                                                                                                                                        | Local storage does not survive container/host loss and does not scale across instances; flagged as a production-only migration (signed-URL object storage) when the PoC graduates.                          |

## Root workspace & tooling — Playwright E2E runner (`playwright.config.ts`)

Decisions for the root Playwright configuration that drives the `packages/web/test/e2e` suite (`make test-e2e` → root `playwright test`).

| What was decided                                                                                                                                                                                                      | What alternatives existed                                                                                                                                         | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                              | What risks it carries                                                                                                                                                                    |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `webkit` is an opt-in Playwright project, pushed into `projects` only when `PLAYWRIGHT_WEBKIT=1`; `chromium` and `firefox` always run. This gates the third of the AAP's literal "three projects" behind an env flag. | Declare `webkit` unconditionally beside `chromium`/`firefox` (the agent-prompt reference snippet); drop WebKit entirely.                                          | The host (Ubuntu 25.10) ships ICU 76 while Playwright's WebKit build links ICU 74, so `webkit` cannot launch locally and an unconditional project turns every `make test-e2e` red. Gating keeps the default run green on this host while preserving the WebKit project definition for supported OSes and the official `mcr.microsoft.com/playwright` image (run with `PLAYWRIGHT_WEBKIT=1`). The Gate 8 fidelity suite is Chromium-only regardless, so visual coverage is unaffected. | WebKit-specific rendering/behaviour is not exercised by default; CI that must cover WebKit has to run on a supported image with `PLAYWRIGHT_WEBKIT=1`.                                   |
| `reporter` is `[['html', { open: 'never' }], ['list']]`, declared inline inside `defineConfig`.                                                                                                                       | The prior stub's env-conditional `process.env.CI ? 'github' : 'list'`; a single `'list'` or `'html'` reporter; extracting the reporter array to a typed variable. | A persistent HTML report (written under `playwright-report/`, which `make clean` already removes) is useful locally and on CI, while `list` gives live progress; `open: 'never'` stops it launching a browser in headless/CI runs. Keeping the array inline lets `defineConfig` contextually type the literals as `ReporterDescription` tuples — extracting to a variable widens them to plain arrays and breaks the type.                                                            | GitHub Actions inline annotations (the `'github'` reporter) are not emitted; they can be added as a third reporter entry under CI if wanted.                                             |
| `expect.toHaveScreenshot` is configured centrally with `maxDiffPixelRatio: 0.02` and `threshold: 0.2`.                                                                                                                | Playwright defaults (no pixel-ratio cap); duplicating per-assertion options in every spec.                                                                        | Gate 8's `screenshot-fidelity.spec.ts` explicitly relies on these tolerances being supplied centrally and does not repeat them on each `toHaveScreenshot` call; a 2% pixel-ratio budget absorbs anti-aliasing and font-rasterisation variance across machines while still catching layout regressions.                                                                                                                                                                                | Genuine visual diffs affecting under 2% of pixels pass; baselines are Chromium-only (other engines rasterise differently), which is why the fidelity suite restricts itself to Chromium. |
| `webServer` is gated behind `PLAYWRIGHT_AUTOSTART`: when set it boots `pnpm --filter @app/api dev` (waited on via `…/api/health`) and `pnpm --filter @app/web dev`; otherwise it is `undefined`.                      | Always autostart both servers from Playwright; never autostart and require `make local` first.                                                                    | `make local` / `make test-e2e` normally start the API and web servers externally, so unconditional autostart would collide with already-running servers; gating delegates the default path to Make while giving CI/standalone runs a one-flag bring-up. Filters use the workspace's `@app/*` scope (not the planning-seed `@blitzy-slack/*`).                                                                                                                                         | When `PLAYWRIGHT_AUTOSTART` is unset the servers must already be listening or specs fail fast on first navigation; `reuseExistingServer` is disabled on CI to force a clean boot.        |
| CI-aware execution profile: `forbidOnly: !!process.env.CI`, `retries: process.env.CI ? 2 : 0`, `workers: process.env.CI ? 1 : undefined`, with `fullyParallel: true`.                                                 | Identical settings for CI and local; multiple parallel workers on CI.                                                                                             | Serialising to a single worker on CI keeps real-time/WebSocket ordering deterministic and avoids cross-test interference on the shared seeded user, while local runs stay fully parallel for speed; CI retries absorb genuinely flaky network timing, and `forbidOnly` stops a stray `test.only` silently shrinking the CI suite.                                                                                                                                                     | Single-worker CI runs are slower, and retries can mask intermittent product bugs — a spec that only passes on retry should still be investigated.                                        |

## `packages/web` — HTTP API client (`src/lib/api-client.ts`)

Decisions for the typed `apiClient` fetch wrapper — the single declaration site for all REST traffic to the API (AAP §0.4.2); direct `fetch` elsewhere is forbidden.

| What was decided                                                                                                                                                                                                                                | What alternatives existed                                                                                                                                                                    | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | What risks it carries                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A custom `ApiError extends Error` carrying `status: number` and `body: unknown` is thrown on every non-2xx response and on network failure.                                                                                                     | Throw the raw `Response`; throw strings; a result-type `{ ok: false, error }` pattern.                                                                                                       | A typed `Error` subclass gives uniform `try/catch` across hooks and components and carries the status plus parsed body for granular error UX (e.g. a 409 conflict and a 422 validation error drive different toasts).                                                                                                                                                                                                                                                                                                                                                | A generic `ErrorBoundary` could swallow it; mitigated by feature hooks catching explicitly and surfacing via Sonner.                                                                          |
| Response validation is opt-in per call via `options.schema` (`ZodType<T>`), not enforced on every call; the client itself imports nothing from `@app/shared`.                                                                                   | Mandatory schema on every call; no response validation at all.                                                                                                                               | Balances safety against `schema.parse` CPU cost — untrusted or critical endpoints (auth, message create) pass a schema while trusted or cosmetic ones (health) may skip it; keeping the client schema-agnostic avoids bundling every shared schema into this base layer.                                                                                                                                                                                                                                                                                             | Drift between the API shape and the client's expectation on schema-less calls; mitigated by integration and E2E coverage of each endpoint.                                                    |
| A 401 response calls `useAuthStore.getState().logout()` before re-throwing `ApiError(401)`.                                                                                                                                                     | Throw only and leave logout to the caller; logout only and swallow the error; route logout through a global event bus.                                                                       | Bundles the side effect (clear the token, disconnect the socket) with error propagation, so the UI redirects to `/login` while the caller can still surface a "session expired" toast.                                                                                                                                                                                                                                                                                                                                                                               | A 401 returned by the login endpoint itself would also call logout; harmless because the store is already in the logged-out state, so `logout()` is a no-op there.                            |
| The DELETE helper is named `del`, not `delete`.                                                                                                                                                                                                 | A quoted `'delete'` property key; a `remove` alias.                                                                                                                                          | `delete` is a reserved word and cannot be a method-shorthand name; the folder spec uses `del` verbatim, and the method body still sends the `DELETE` verb.                                                                                                                                                                                                                                                                                                                                                                                                           | A minor naming mismatch with the HTTP verb; mitigated by the correct verb inside the request.                                                                                                 |
| The JWT is read at request time via `useAuthStore.getState().token`, never cached at module load.                                                                                                                                               | Cache the token at module load; cache it at the first authenticated call.                                                                                                                    | The auth store is the single source of truth for the token, and a user may sign in after this module loads — a cached initial `null` would freeze the client to the logged-out state.                                                                                                                                                                                                                                                                                                                                                                                | One extra `getState()` call per request (sub-microsecond); negligible.                                                                                                                        |
| DEVIATION from the verbatim AAP source: three type assertions in the seed snippet — `(body as { error: unknown })`, `(body as { message: unknown })`, and `body as FormData` — were dropped (left as `body.error`, `body.message`, and `body`). | Keep the verbatim casts and suppress `@typescript-eslint/no-unnecessary-type-assertion` with an `eslint-disable` comment or a per-file config override; weaken the workspace ESLint ruleset. | Under the project's actual toolchain (TypeScript 5.9.3 with `no-unnecessary-type-assertion` from `recommendedTypeChecked`), control-flow narrowing — `'error' in body`, `'message' in body`, and the `isFormData` const-alias guard — already yields the exact target type, so the casts are reported as unnecessary and fail `eslint --max-warnings 0` (Rule 3). Removal is type-identical (confirmed by `tsc --noEmit` and by the rule's own analysis) and is exactly what `eslint --fix` would apply; Rule 3 outranks byte-for-byte fidelity to the seed snippet. | The file is no longer byte-identical to the AAP snippet; the runtime behaviour and the public type surface are unchanged, and the deviation is confined to removing provably redundant casts. |
| A new ambient `packages/web/src/vite-env.d.ts` augments the global `ImportMetaEnv` with `VITE_API_URL`, `VITE_WS_URL`, and `VITE_APP_URL` typed as `readonly string`.                                                                           | A triple-slash `/// <reference types="vite/client" />` template file; inline-typing the env read inside `api-client.ts`; leaving the keys as `any`.                                          | `api-client.ts` is the first `import.meta.env` consumer in the web source, and Vite's default `ImportMetaEnv` index signature types `VITE_API_URL` as `any`, which trips the `no-unsafe-*` rules on `.replace()` and fails Rule 3. A global interface merge (no `import`/`export`, and no triple-slash since `vite/client` is already in the tsconfig `types` array) types the keys as `string` without altering the verbatim client; triple-slash was avoided because `@typescript-eslint/triple-slash-reference` would flag it.                                    | Any future `VITE_*` variable must be added to this interface to stay typed; low maintenance and standard Vite practice.                                                                       |
| DEVIATION from the verbatim AAP snippet: `src/lib/api-client.ts` was reformatted with the repository's Prettier config (`printWidth: 100`), collapsing several multi-line expressions and method signatures onto single lines.                  | Commit the file with the snippet's narrower (~80-col) wrapping; add the file to `.prettierignore`; widen Prettier's `printWidth` to preserve the original wrapping.                          | The workspace ships `eslint-config-prettier` plus `format` / `format:check` scripts, so Prettier is the authoritative formatter and `make format` (`prettier --write .`) would reformat the file regardless; committing a file that fails `prettier --check` would leave a formatting violation. The edit is whitespace-only — `eslint --max-warnings 0` and all 39 ad-hoc behavioral assertions still pass — so repo-wide formatting consistency outranks byte-for-byte fidelity to the snippet's wrapping.                                                         | The file is no longer byte-identical to the AAP snippet; runtime behavior and the public type surface are unchanged, and the deviation is limited to line wrapping.                           |

## `packages/api` — Configuration (Redis clients, `config/redis.ts`)

Decisions for the three ioredis client connections and the graceful-shutdown helper exported by `config/redis.ts`. Pattern A exports the raw clients; the Socket.io Redis adapter is assembled at the call site in `src/index.ts`, so this module has zero coupling to `@socket.io/redis-adapter`.

| What was decided                                                                                                                                                                                                                                                   | What alternatives existed                                                                                                                        | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | What risks it carries                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pattern A — `redis.ts` exports the raw `pubClient`/`subClient`/`redisClient` ioredis instances and the adapter is assembled at the call site (`io.adapter(createAdapter(pubClient, subClient))` in `src/index.ts`)                                                 | Pattern B — export a pre-built adapter (or an adapter factory) from `redis.ts`                                                                   | Keeps the Socket.io wiring centralized in `index.ts` and leaves `redis.ts` coupled only to `ioredis`, `./env.js`, and `./logger.js` (no `socket.io`/`@socket.io/redis-adapter` import), so the module stays at the bottom of the dependency tree and is trivially testable in isolation                                                                                                                                                                                                                   | Consumers must remember to call `createAdapter(pubClient, subClient)` in `index.ts`; the adapter wiring is not enforced by this module's type surface                                                                                                                     |
| Three DISTINCT `new Redis(env.REDIS_URL, baseOptions)` connections — `pubClient`, `subClient`, and `redisClient` (presence)                                                                                                                                        | Reuse a single client for publish, subscribe, and presence; or build `subClient` as `pubClient.duplicate()`                                      | `@socket.io/redis-adapter` REQUIRES independent pub and sub connections (a Redis connection in subscriber mode cannot serve ordinary commands), and keeping presence on its own `redisClient` avoids head-of-line blocking behind the subscriber connection (AAP §0.4.5, Rule 2); `.duplicate()` is intentionally avoided so each connection is constructed identically and independently from `env.REDIS_URL`                                                                                            | 3× the connection count to Redis (acceptable at PoC scale) and three sockets to close on shutdown, handled by `disconnectRedisClients()`                                                                                                                                  |
| `maxRetriesPerRequest: 3` on the shared `baseOptions`                                                                                                                                                                                                              | The ioredis default (`20`); `null` (retry a command indefinitely)                                                                                | Fails a command after three retries so a persistent Redis problem surfaces quickly to operators instead of silently hanging requests; transient blips are still absorbed by `reconnectOnError`                                                                                                                                                                                                                                                                                                            | A Redis hiccup spanning more than three retries can fail an in-flight command; mitigated by `reconnectOnError` reconnecting on `READONLY`/`ECONNRESET`/`ETIMEDOUT`                                                                                                        |
| `lazyConnect: false` (eager connect at construction) together with `enableReadyCheck: true`                                                                                                                                                                        | `lazyConnect: true` (defer the connection until the first command)                                                                               | Eager connection surfaces a Redis misconfiguration at startup rather than at the first request; `make local` waits for the Redis healthcheck before starting the API, so the eager connect succeeds on a clean bring-up (Gate 1)                                                                                                                                                                                                                                                                          | Importing the module raises connection errors at startup if Redis is unreachable (the intended fail-fast); any consumer importing `redis.ts` opens three TCP connections as a load-time side effect, so `index.ts` must call `disconnectRedisClients()` on SIGTERM/SIGINT |
| URL credentials stripped via a local `safeUrl()` helper, and only the resulting `REDIS_URL_SAFE` is ever logged; the `error` event handler logs `err.message` only                                                                                                 | Log the raw `env.REDIS_URL`; or log nothing about the URL at all                                                                                 | The raw connection string may embed credentials (AAP §0.8.4 security baseline), so logging it verbatim would leak secrets; stripping `username`/`password` via the Node `URL` API keeps the diagnostic value (host, port, db) without the auth portion, and never stringifying the whole error keeps connection details out of the logs                                                                                                                                                                   | A small extra surface (one helper); if `env.REDIS_URL` is unparseable the log shows the constant `[invalid url]` placeholder rather than a real value                                                                                                                     |
| DEVIATION from the verbatim agent-prompt snippet: in `disconnectRedisClients()` a rejected settle result is logged as a narrowed string (`result.reason instanceof Error ? result.reason.message : String(result.reason)`) instead of the raw `err: result.reason` | Log `err: result.reason` verbatim and suppress the resulting lint error with an `// eslint-disable` comment; weaken the workspace ESLint ruleset | `PromiseRejectedResult.reason` is typed `any`, so `{ err: result.reason }` trips `@typescript-eslint/no-unsafe-assignment` and fails `eslint --max-warnings 0` (Rule 3) — verified empirically with a throwaway probe. Narrowing to the message is also strictly more compliant with the §0.8.4 baseline ("log `err.message` only, never the whole error"), and `@ts-ignore`/`eslint-disable` are forbidden by Rule 3. Runtime behaviour is preserved — a rejection is still logged with its client label | The logged value is the error message string, not the original error object; acceptable because §0.8.4 forbids serialising the whole error anyway                                                                                                                         |

## `packages/api` — Messages service (`services/messages.service.ts`)

| What was decided                                                                                                                                                                                                 | What alternatives existed                                                                                | Why this choice was made                                                                                                                                                                                                                                                                                                    | What risks it carries                                                                                                                                                             |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Threads are single-level — `sendMessage` rejects a `parentId` whose target is itself a reply (`parent.parentId !== null` → `ValidationError`)                                                                    | Allow arbitrary reply nesting (a reply to a reply)                                                       | Slack's thread model is single-depth (AAP §0.6.3 thread side panel); a flat `parentId` self-reference keeps `getReplies` a single `where: { parentId }` lookup and the thread UI a flat list                                                                                                                                | A future need for nested threads would require schema and query changes; explicitly out of scope per AAP §0.7.2                                                                   |
| `addReaction` is an idempotent `upsert` on the `messageId_userId_emoji` composite (empty `update: {}`), and `removeReaction` is a `delete` whose Prisma `P2025` ("record to delete does not exist") is swallowed | Read the reaction first, then conditionally `create`/`delete` (check-then-act)                           | The single-statement upsert/delete is atomic and race-free and lets the client call add/remove blindly without first fetching state; the `@@unique([messageId, userId, emoji])` constraint guarantees no duplicate rows                                                                                                     | The idempotent remove path constructs and discards one `PrismaClientKnownRequestError` when the reaction was absent; a negligible cost for the simpler client contract            |
| Both reaction mutations re-fetch the full message (`findUniqueOrThrow` with author/reactions/file/`_count` includes) to build the returned `MessageWithAuthor`                                                   | Mutate the in-memory reaction array and return the message without a second query                        | The database stays the single source of truth for the post-mutation reaction set, so the DTO can never drift from persisted state, and it reuses the exact include shape used by `sendMessage`/`getReplies`                                                                                                                 | One extra `SELECT` per reaction operation; negligible at PoC scale and well within the Gate 9 latency budget                                                                      |
| A file may be attached only by its uploader — `sendMessage` throws `ForbiddenError` when `File.uploadedById !== authorId`                                                                                        | Allow any authenticated user to attach any previously uploaded `File` by id                              | Prevents attachment hijacking (user A uploads a file, user B references its id on their own message); combined with `Message.fileId @unique`, a file binds to exactly one message and one owner                                                                                                                             | A legitimate multi-step "upload then send" flow must run under the same authenticated session; enforced and expected                                                              |
| `getReplies` returns replies ASC (oldest-first), whereas channel/DM timelines paginate DESC (newest-first)                                                                                                       | Use one consistent ordering direction across all message reads                                           | A thread panel reads top-to-bottom from the parent while a channel reads bottom-to-top with infinite scroll; each endpoint matches its UI's natural reading order                                                                                                                                                           | Consumers must know the per-endpoint direction; documented in the function JSDoc and here                                                                                         |
| `getReplies` is unpaginated — it returns the entire reply list for a parent                                                                                                                                      | Apply the same cursor pagination used for channel/DM message history                                     | Threads are typically short and the UI loads the whole thread when the side panel opens, so pagination would add complexity with no PoC benefit                                                                                                                                                                             | A pathologically long thread loads all replies at once; cursor pagination can be layered in later without changing the wire DTO                                                   |
| DEVIATION from this file's `depends_on_files` metadata: the domain error classes (`ForbiddenError`, `NotFoundError`, `ValidationError`) are imported from `../middleware/errors.js`                              | Import them from `packages/api/src/services/errors.ts`, the path named in the file's dependency metadata | No `services/errors.ts` exists; the committed error classes live in `middleware/errors.ts` (see the _Service errors_ section above) and `search.service.ts` already imports them from there. Importing the same named symbols from their actual location is the only compilable path and keeps the service layer consistent | The dependency metadata names a path that differs from the real one; this row makes the reconciliation explicit so the deviation is not treated as a defect (Explainability rule) |

## `packages/api` — Channels service (`services/channels.service.ts`)

| What was decided                                                                                                                                                                                                                                                                                                                                                                                                       | What alternatives existed                                                                                                                                                                                         | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | What risks it carries                                                                                                                                                                                                                                                                                                                |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| The channel creator is made an `owner` by inserting the `Channel` row and the creator's `ChannelMember` row (with `role: 'owner'`) inside a single `prisma.$transaction`                                                                                                                                                                                                                                               | Two sequential un-transacted writes; a post-insert `update` of the member row; relying on the schema default `role: 'member'` and never marking an owner                                                          | Atomicity guarantees a channel never exists without its owner membership, and the explicit `'owner'` overrides the schema's `'member'` default in one round-trip. A non-transactional pair could leave an orphaned channel with no members if the second insert failed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The role vocabulary is just `'owner'`/`'member'` (no admin tier — out of scope per AAP §0.7.2); a richer role model would need a schema change                                                                                                                                                                                       |
| `joinChannel` refuses PRIVATE channels with `ForbiddenError`; only public channels are self-joinable                                                                                                                                                                                                                                                                                                                   | Allow self-join of private channels; implement an invite/token flow; silently no-op for private channels                                                                                                          | The PoC has no invitation flow (AAP §0.7.2), and private-channel membership is established at creation time with members pre-baked. Blocking self-join keeps the private-channel ACL meaningful — otherwise any user could join any private channel by id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Private channels can only gain members through creation-time seeding until an invite flow is added; this is an accepted PoC scope limitation, surfaced to the user as a 403                                                                                                                                                          |
| The message-history cursor is `base64url(JSON({ createdAt, id }))` — an opaque compound (createdAt, id) token                                                                                                                                                                                                                                                                                                          | A bare message `id`; a numeric `OFFSET`; a signed JWT cursor; a raw (unencoded) JSON string                                                                                                                       | A compound (createdAt, id) key gives a stable total order even when several messages share a millisecond timestamp (cuid `id` breaks ties), matching the `ORDER BY createdAt DESC, id DESC` contract of AAP §0.8.4. `base64url` is URL-safe (no `+`, `/`, `=` to escape in a query string) and trivially decodable for debugging. This mirrors the identical cursor used by `dms.service.ts` for one pagination contract across channels and DMs                                                                                                                                                                                                                                                                                                                                                       | The cursor embeds the `createdAt` representation, so a change to how timestamps are stored could invalidate in-flight cursors; acceptable for a PoC and versionable later. The token is opaque but NOT authenticated — a client could forge one, but it only shifts that same caller's own page window, leaking nothing              |
| Pagination uses a `take: limit + 1` "peek" to compute `hasMore`/`nextCursor`, rather than a separate `COUNT`                                                                                                                                                                                                                                                                                                           | A second `COUNT(*)` query over the same `WHERE`; offset pagination; always returning a cursor                                                                                                                     | Fetching one extra row determines whether an older page exists in a single query, avoiding a second round-trip and the staleness a `COUNT` can show under concurrent inserts. `nextCursor` is emitted only when the peek row is present, so the client stops paging exactly when the timeline is exhausted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | One surplus row is fetched per page and discarded — negligible at the 50-row PoC page size                                                                                                                                                                                                                                           |
| The channel timeline returns only top-level messages (`where: { parentId: null }`); thread replies are excluded                                                                                                                                                                                                                                                                                                        | Return every message flat (replies inline with parents); fetch replies and merge in memory                                                                                                                        | AAP §0.6.3 renders replies in a separate thread side-panel (served by `messages.service.getReplies`), so the channel view shows only roots. Filtering in the query keeps the page size meaningful (a busy thread cannot crowd out other conversations)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | A reply is invisible until its thread panel is opened; acceptable and consistent with the Slack UI model                                                                                                                                                                                                                             |
| `leaveChannel` translates Prisma `P2025` ("record to delete does not exist") into `NotFoundError`, making the operation idempotent from the client's view                                                                                                                                                                                                                                                              | Let `P2025` propagate as a 500; pre-check membership with a `findUnique` then delete (check-then-act); return success even when no row was deleted                                                                | "Leave a channel you are not a member of" is semantically a 404 (the membership resource does not exist), and catching the single delete's `P2025` avoids a check-then-act race in one statement. Returning success silently would hide a genuinely wrong channel id from the caller                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Constructing and discarding one `PrismaClientKnownRequestError` on the not-a-member path; negligible cost for the simpler, race-free contract                                                                                                                                                                                        |
| DEVIATION from this file's `depends_on_files` / agent-prompt metadata: error classes are imported from `../middleware/errors.js` and workspace types from the `@app/*` scope (not `./errors.js` / `@blitzy-slack/*`); `ChannelMember` and `ChannelWithMembers` are NOT imported; `listChannels` maps each row to `ChannelSummary` as `{ id, name, isPrivate }` only; reaction summaries use the `hasCurrentUser` field | Import error classes from a non-existent `services/errors.ts`; import every type named in the metadata regardless of use; add a `memberCount` / `description` to the summary; name the reactor flag `reactedByMe` | No `services/errors.ts` exists — the committed classes live in `middleware/errors.ts`, and the sibling `dms`/`messages`/`search` services already import them from there. Packages are scoped `@app/*` (see the package-scope rows above), not `@blitzy-slack/*`. Importing `ChannelMember`/`ChannelWithMembers` or emitting `memberCount` would be a Rule 3 violation: the unused imports trip `@typescript-eslint/no-unused-vars` and `noUnusedLocals`, and `memberCount`/`description` are absent from the actual `ChannelSummary` shape (`{ id, name, isPrivate, unreadCount? }`), so the object would fail to typecheck. The actual `ReactionSummary` field is `hasCurrentUser`. Matching the shipped contracts is the only path that compiles under `tsc --noEmit` and `eslint --max-warnings 0` | The metadata names paths/types/shapes that differ from the real ones; this row makes the reconciliation explicit so the deviation is not treated as a defect (Explainability rule). `memberCount` is therefore unavailable from the list endpoint and must come from a future channel-detail endpoint returning `ChannelWithMembers` |

## `packages/api` — Files service (`services/files.service.ts`)

| What was decided                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | What alternatives existed                                                                                                  | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | What risks it carries                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| File access control is derived from message visibility: `getFile` grants access iff a `Message` referencing the file lives in a channel the caller is a member of or a DM the caller participates in — there is no separate `FileAccess(fileId, userId)` table                                                                                                                                                                                                                       | An explicit `FileAccess` share table; an ACL list stored on the `File` row                                                 | Messages already enforce channel/DM ACL, and `Message.fileId @unique` binds a file to exactly one message, so file visibility can inherit from its containing message with no extra schema or sync logic; a single `message.findFirst` with an `OR` over `channel.members.some` / `dm.participants.some` decides access in one query                                                                                                                                                                                                                | A file uploaded but never attached to a message is reachable only by its uploader (see next row); were the `@unique` constraint ever relaxed, inheritance could over-grant — the constraint prevents this today |
| The uploader (`File.uploadedById === userId`) is granted access unconditionally and the ACL message lookup is short-circuited                                                                                                                                                                                                                                                                                                                                                        | Require the uploader to also be a member/participant of the referencing message's channel/DM before serving their own file | A user who uploads a file but has not yet sent the message must not lose access to their own upload (the two-step "upload then send" flow), and skipping the lookup on the owner path saves a query                                                                                                                                                                                                                                                                                                                                                 | Marginal — the uploader already holds a valid session token; an uploaded-but-unsent file stays private to its uploader, which is the intended behaviour                                                         |
| `saveUploadedFile` re-checks `multerFile.size` against `MAX_FILE_SIZE_MB * 1024 * 1024` and throws `ForbiddenError` (not `ValidationError`) when exceeded                                                                                                                                                                                                                                                                                                                            | Trust multer's `limits.fileSize` entirely and omit the service check; throw `ValidationError` instead                      | Defense-in-depth against a misconfigured upload middleware: multer should reject oversize uploads first (HTTP 413), so reaching this branch is a security-layer failure the client cannot trigger through normal input — 403 fits better than a 400 input error — and the check is a single numeric comparison logged at `warn`                                                                                                                                                                                                                     | None of note — under correct multer configuration the branch is unreachable; it exists only to fail closed on misconfiguration                                                                                  |
| `getFile` returns `{ file, diskPath }` with `diskPath = path.resolve(env.FILE_UPLOAD_PATH, record.storedName)`; the route streams via `res.sendFile(diskPath)`, and the service never touches `req`/`res`/sockets nor reads file content (`node:fs` is not imported)                                                                                                                                                                                                                 | Stream the file from inside the service (accept `res`); use `path.join` instead of `path.resolve`                          | Keeps the service HTTP-agnostic and unit-testable, consistent with the sibling services that emit no I/O of their own; `path.resolve` yields a canonical absolute path for `res.sendFile`, and because `storedName` is the multer-generated filename (not user input) the resolved path is traversal-safe                                                                                                                                                                                                                                           | The route layer must stream the returned `diskPath`; documented in the function JSDoc and the route contract                                                                                                    |
| `toFileDto` sets the DTO `url` to the relative API route `/api/files/${id}`, never the on-disk path                                                                                                                                                                                                                                                                                                                                                                                  | Return the absolute disk path; return a fully-qualified URL including host                                                 | The web client composes the absolute URL as `${VITE_API_URL}${url}`, the API route streams the bytes behind that path, and a relative path leaks nothing about the server filesystem layout — the standard REST pattern already used by `messages.service.ts` for attachment DTOs                                                                                                                                                                                                                                                                   | None — the client owns origin composition via `VITE_API_URL`                                                                                                                                                    |
| DEVIATION from this file's `depends_on_files` / agent-prompt metadata: error classes (`ForbiddenError`, `NotFoundError`) are imported from `../middleware/errors.js` and workspace symbols from the `@app/*` scope (not `./errors.js` / `@blitzy-slack/*`); `toFileDto` emits the full shared `FileAttachment` shape — all eight fields including `storedName` and a nullable `uploadedById` (`string \| null`) — correcting the agent-prompt draft mapper that omitted `storedName` | Import from a non-existent `services/errors.ts` under the `@blitzy-slack/*` scope; emit the draft DTO without `storedName` | No `services/errors.ts` exists — the committed classes live in `middleware/errors.ts` and every sibling service imports them from there; packages are scoped `@app/*` (see the package-scope rows above). The shared `FileAttachment` interface requires `storedName` and types `uploadedById` as `string \| null`, so the draft mapper omitting `storedName` would not typecheck; emitting all eight fields (matching the file mapping in `messages.service.ts`) is the only path that compiles under `tsc --noEmit` and `eslint --max-warnings 0` | The metadata names a path/scope and a DTO shape that differ from the real ones; this row makes the reconciliation explicit so the deviation is not treated as a defect (Explainability rule)                    |

## `packages/api` — Authentication service (`services/auth.service.ts`)

| What was decided                                                                                                                                                                                                                                                                                                                                                  | What alternatives existed                                                                                                                                                                    | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | What risks it carries                                                                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Password hashing imports `bcryptjs` (`import bcrypt from 'bcryptjs'`; `bcrypt.hash(pw, env.BCRYPT_ROUNDS)` / `bcrypt.compare(pw, hash)`) — this service is the FIRST API consumer of the hashing library                                                                                                                                                          | Native `bcrypt@5.1.x` (named by this file's `external_imports` metadata); `argon2`                                                                                                           | The CP2 dependency-hardening row already swapped the manifest to `bcryptjs` (native `bcrypt` pulled a high-severity `tar` advisory), and only `bcryptjs` is installed/symlinked for `@app/api`; it exposes the identical async `hash`/`compare` surface and ships its own ESM types, so it default-imports cleanly under `module: NodeNext`. Importing native `bcrypt` would fail to resolve and break `eslint --max-warnings 0` / `tsc` (Rule 3)                                                                                                                                                                                                      | `bcryptjs` is ~30% slower than native at the same cost factor; acceptable at PoC login volume and `BCRYPT_ROUNDS` is env-tunable                                                                                                              |
| JWTs are signed AND verified with HS256 only — `signToken` sets `algorithm: 'HS256'`, `verifyToken` pins `algorithms: ['HS256']` — and the payload carries only `sub` (user id) + `email`                                                                                                                                                                         | RS256 (asymmetric, rotation-friendly); accept the library's default algorithm set on verify; embed role/profile claims                                                                       | A single shared secret (`env.JWT_SECRET`, Zod `min(32)`) fits the single-process PoC and enables a stateless Socket.io handshake reusing the same verifier (AAP §0.8.4 JWT scope discipline); pinning the accepted algorithm blocks algorithm-confusion attacks (an `alg:none` or RS/HS-swap token is rejected); a minimal payload avoids leaking profile or secret data into the token                                                                                                                                                                                                                                                                | A compromised `JWT_SECRET` is a total auth bypass (mitigated by the 32-char floor); HS256 cannot do per-consumer key rotation like RS256 — out of PoC scope                                                                                   |
| `register` does NOT wrap `prisma.user.create` in try/catch, so a duplicate-email `PrismaClientKnownRequestError` (`P2002`) propagates UNWRAPPED; consequently the `Prisma` namespace (listed in this file's `internal_imports` metadata) is intentionally NOT imported, and the domain error classes come from `../middleware/errors.js` under the `@app/*` scope | Catch `P2002` and rethrow a `ConflictError`; import the `Prisma` namespace to reference the error type; import errors from a `./errors.js` / `services/errors.ts` under `@blitzy-slack/*`    | Rule 4 idempotency requires the seed flow to receive HTTP 409 on duplicate email; the error-handler middleware maps `instanceof Prisma.PrismaClientKnownRequestError && code === 'P2002'` → 409, so this service must let the raw error through and never inspects it here — importing the unused `Prisma` namespace would fail `no-unused-vars` (Rule 3). No `services/errors.ts` exists; the committed classes live in `middleware/errors.ts` and every sibling service imports them from there under `@app/*`                                                                                                                                       | The 409 contract depends on the error-handler recognising the Prisma error class (documented in that file); one metadata-listed symbol (`Prisma`) is intentionally omitted, reconciled by this row so it is not treated as a defect           |
| `login` throws a single uniform `UnauthorizedError('Invalid email or password')` for BOTH unknown-email and wrong-password, and runs `bcrypt.compare` only when a user row exists (`user !== null && (await bcrypt.compare(...))`)                                                                                                                                | Distinct messages per failure cause; always compare against a precomputed dummy hash when the user is absent for fully constant-time behaviour                                               | A uniform message prevents account enumeration through differing API responses; gating the compare on user existence avoids a pointless hash when no account matches                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | The short-circuit leaks a sub-millisecond timing signal on the no-user path (no hash computed); acceptable for a PoC, and a dummy-hash compare can be layered later if timing-attack hardening is required                                    |
| `toUserDto` returns the shared `UserResponse` (= `Omit<User, 'passwordHash'>`) — the type backing `AuthResult.user` and `getCurrentUser` — built by an explicit field map with ISO-8601 date strings; the ONLY type assertion in the file is `env.JWT_EXPIRES_IN as SignOptions['expiresIn']`                                                                     | Return the shared `User` named by this file's metadata (which INCLUDES `passwordHash`); destructure-and-rest to drop `passwordHash`; silence the `expiresIn` type mismatch with `@ts-ignore` | The shared `User` carries `passwordHash`, so returning it would both leak the hash and fail to typecheck against the explicit map; `UserResponse` is the documented self-view (see the shared-contracts section). An explicit map (vs `{ passwordHash: _, ...rest }`) avoids an unused-binding lint error and is auditable. jsonwebtoken types `expiresIn` as a narrow `StringValue \| number`, so a validated free-form duration string needs one asserted conversion — `@ts-ignore` is forbidden by Rule 3, and `jwt.verify`'s `string \| JwtPayload` result is narrowed via `typeof` checks (through a `Record<string, unknown>` view) with no cast | `UserResponse` must stay aligned with the Prisma model — it derives from `User` via `Omit`, so one change propagates; a malformed `JWT_EXPIRES_IN` would fail at sign time, surfaced immediately on first token issuance rather than silently |

## `packages/api` — Error-handler middleware (`middleware/error-handler.ts`)

| What was decided                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | What alternatives existed                                                                                                                                                                                                                                       | Why this choice was made                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | What risks it carries                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| DEVIATION from this file's agent-prompt "EXACT content" / metadata: the `Prisma` namespace is imported from `@app/db` (not `@blitzy-slack/db`), the domain error classes from `./errors.js` (the sibling `middleware/errors.ts`, not `../services/errors.js`), and `jsonwebtoken` / `multer` are imported as DEFAULT exports (`import jwt from 'jsonwebtoken'`, `import multer from 'multer'`) with `jwt.TokenExpiredError`, `jwt.JsonWebTokenError`, and `multer.MulterError` accessed as members for the `instanceof` guards | Use the literal metadata: the `@blitzy-slack/*` scope, a `../services/errors.js` path, and ESM NAMED runtime imports `import { JsonWebTokenError, TokenExpiredError } from 'jsonwebtoken'` and `import { MulterError } from 'multer'`                           | No `@blitzy-slack/*` package and no `services/errors.ts` exist — packages are scoped `@app/*` (see the package-scope rows above) and the committed error classes live in `middleware/errors.ts`, from which every sibling service imports them. `jsonwebtoken` is a CommonJS module whose error classes are NOT lexer-detectable as ESM named exports under `module: NodeNext`; a native-Node-ESM probe proved `import { JsonWebTokenError } from 'jsonwebtoken'` throws "Named export not found" at runtime, which would crash both `node dist/index.js` and `tsx`. The default-import + member-access form (matching `auth.service.ts` and `upload.ts`) is the only shape that both compiles under `tsc` / `eslint --max-warnings 0` and runs; `multer` is converted likewise for native-ESM + jest-ESM robustness and convention parity, while `zod` (true ESM) and `@app/db` (workspace TS source) keep their named imports | The metadata names a scope, a path, and an import style that differ from the shipped ones; this row reconciles them so the deviation is not treated as a defect (Explainability rule). The `instanceof` semantics are unchanged — `jwt.TokenExpiredError`/`multer.MulterError` are the same runtime constructors the named imports would have referenced |
| `req.log` is typed by loading pino-http's `declare module "http"` augmentation through a bare type-only import `import type {} from 'pino-http'` placed first in the file                                                                                                                                                                                                                                                                                                                                                      | A side-effect `import 'pino-http'` (incurs a runtime module load); a local `declare module 'http'` block duplicating pino-http's own declaration; relying on the not-yet-created `request-logger.ts` to import pino-http and activate the augmentation globally | `resolveLogger` reads `req.log` (the per-request child logger that the pino-http request-logger attaches), which is typed only when pino-http's augmentation of `http.IncomingMessage` (the interface Express `Request` extends) is part of the program. No compiled file currently imports pino-http and `tsconfig` `types` is `["node", "jest"]`, so the augmentation is otherwise dormant and `req.log` would fail to typecheck. A type-only empty import activates it with ZERO runtime cost (fully elided by both `tsc` and `ts-jest`) and does not depend on `request-logger.ts` existing yet                                                                                                                                                                                                                                                                                                                             | Reads like an unused import to a casual reader; the adjacent code comment states its purpose to prevent accidental removal. Becomes redundant — but harmless — once `request-logger.ts` imports pino-http for real                                                                                                                                       |
| `err instanceof jwt.TokenExpiredError` is checked BEFORE `err instanceof jwt.JsonWebTokenError`, mapping to `code: 'token_expired'` vs `code: 'token_invalid'` respectively (both HTTP 401)                                                                                                                                                                                                                                                                                                                                    | A single `JsonWebTokenError` branch that sub-inspects the message/name; the reverse check order                                                                                                                                                                 | `TokenExpiredError` extends `JsonWebTokenError`, and `instanceof` matches a subclass instance against the superclass test, so checking the superclass first would misclassify every expired token as merely "invalid" and lose the distinct `token_expired` signal the client needs to trigger a silent re-login                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | None                                                                                                                                                                                                                                                                                                                                                     |
| Unknown errors, non-`P2002`/`P2025` Prisma known errors, and `PrismaClientValidationError` all return a GENERIC 500 body (`InternalServerError` with a fixed message); the raw `err.message`, `err.stack`, and Prisma `err.meta` are logged at `error` but NEVER placed in the response, while routine 4xx client errors are logged at `warn`                                                                                                                                                                                  | Echo `err.message` / `err.stack` / `meta` in the response for easier client-side debugging; log every error at a single level                                                                                                                                   | Leaking internal text (e.g. `connect ECONNREFUSED`, SQL fragments, or the conflicting column names Prisma puts in `meta.target`) exposes infrastructure to attackers; operators still receive the full detail via the structured Pino line (Gate 10). The warn-for-4xx / error-for-5xx split lets dashboards alert on server bugs (5xx) without noise from expected client errors (bad inputs, expired tokens)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Less helpful client UX on unexpected 500s; mitigated by always-on structured logging keyed by `reqId`/`userId` for operator forensics                                                                                                                                                                                                                    |
| `MulterError` with `code === 'LIMIT_FILE_SIZE'` maps to 413 Payload Too Large; all other multer codes (`LIMIT_FIELD_COUNT`, `LIMIT_UNEXPECTED_FILE`, …) map to 400 Bad Request                                                                                                                                                                                                                                                                                                                                                 | Map every `MulterError` to 500; map all multer errors (including the size cap) to 400                                                                                                                                                                           | The size cap is the enforcement point for `MAX_FILE_SIZE_MB` and earns the specific, semantically-correct 413; the remaining multer errors are client multipart-input mistakes (4xx), not server faults (5xx)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | A multer error that actually reflects a server misconfiguration is surfaced as a 400; mitigated by the `warn` log line carrying the real multer `code`                                                                                                                                                                                                   |
| The handler is the SINGLE centralized error formatter (`app.use(errorHandler)` mounted last); inline 5xx payloads use `satisfies ErrorPayload` rather than an `as` cast; `resolveLogger` returns `req.log ?? appLogger`                                                                                                                                                                                                                                                                                                        | Per-route try/catch with hand-written JSON; an `as ErrorPayload` cast; always use the bare logger singleton, or hard-require `req.log` to be present                                                                                                            | One handler yields a uniform JSON shape and removes per-route duplication (AAP §0.8.4); Express 5 forwards both sync throws and async rejections to it automatically. `satisfies` validates each literal against the `ErrorPayload` contract without widening the type or suppressing errors (Rule 3-safe, unlike `as`). Nullish-coalescing the logger keeps the handler resilient to unusual middleware ordering (a low-level failure before the request-logger ran) without crashing on `undefined.error`, and `??` (not `                                                                                                                                                                                                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                                                          | `) treats only nullish values as absent so a validly-disabled logger is still used | All errors funnel through one hot function; mitigated by the trivial `instanceof` type guards that carry no allocation or I/O |
